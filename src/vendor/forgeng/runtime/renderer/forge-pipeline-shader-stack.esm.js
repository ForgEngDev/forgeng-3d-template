var e=Object.defineProperty,t=(t,a,r)=>((t,a,r)=>a in t?e(t,a,{enumerable:!0,configurable:!0,writable:!0,value:r}):t[a]=r)(t,"symbol"!=typeof a?a+"":a,r);import{i as a,l as r}from"./chunks/ShadowShaderAbi.js";const o=a(r),l=a("$$SHADOW_ABI$$const SHADOW_DIRECTIONAL_CASTER_VERT_MATCH:f32=0.020;struct ModelUniform{matrix:mat4x4<f32>,color:vec4<f32>,receiveShadows:f32,alphaCutoff:f32,shadowReceiverBiasMul:f32,padding:f32,};@group(1)@binding(0)var<uniform>model:ModelUniform;struct VertexInput{@location(0)position:vec3<f32>,@location(1)normal:vec3<f32>,};fn getShadowLight()->LightUniforms{let count=max(i32(uLights.count.x),1);return uLights.lights[clamp(i32(uShadowPass.packedLightIndex),0,count-1)];}fn getShadowNormalBiasBase(light:LightUniforms)->f32{if(light.position.w>0.5){return max(light.position.y,0.0);}return max(uShadowSettings.normalBias,0.0);}fn inverseMat3(m:mat3x3<f32>)->mat3x3<f32>{let a=m[0];let b=m[1];let c=m[2];let r0=cross(b,c);let r1=cross(c,a);let r2=cross(a,b);let det=dot(a,r0);let safeDet=select(det,1.0,abs(det)<0.000001);return mat3x3<f32>(r0,r1,r2)*(1.0/safeDet);}@vertex fn vs_shadow(input:VertexInput)->@builtin(position)vec4<f32>{let matrixIndex=i32(min(uShadowPass.matrixIndex,23u));let shadowMatrix=uShadowMatrices.matrices[matrixIndex];let normalMatrix=transpose(inverseMat3(mat3x3<f32>(model.matrix[0].xyz,model.matrix[1].xyz,model.matrix[2].xyz)));let worldNormal=normalize(normalMatrix*input.normal);let worldPos4=model.matrix*vec4<f32>(input.position,1.0);let shadowLight=getShadowLight();let worldLightDir=normalize(-shadowLight.direction.xyz);let ndl=clamp(dot(worldNormal,worldLightDir),-1.0,1.0);let sinNL=sqrt(max(0.0,1.0-ndl*ndl));let directionalCasterVertScale=select(1.0,SHADOW_DIRECTIONAL_CASTER_VERT_MATCH,shadowLight.direction.w<0.5);let casterNormalBias=getShadowNormalBiasBase(shadowLight)*sinNL*directionalCasterVertScale;let biasedWorldPos4=vec4<f32>(worldPos4.xyz-worldNormal*casterNormalBias,worldPos4.w);return shadowMatrix*biasedWorldPos4;}\n"),i=a("$$SHADOW_ABI$$const SHADOW_DIRECTIONAL_CASTER_VERT_MATCH:f32=0.020;struct EntityInstance{worldMatrix:mat4x4<f32>,color:vec4<f32>,receiveShadows:f32,alphaCutoff:f32,shadowReceiverBiasMul:f32,meshId:u32,materialId:u32,};@group(1)@binding(0)var<storage,read>entityBuffer:array<EntityInstance>;@group(1)@binding(1)var<storage,read>visibleIndices:array<u32>;struct VertexInput{@location(0)position:vec3<f32>,@location(1)normal:vec3<f32>,};fn getShadowLight()->LightUniforms{let count=max(i32(uLights.count.x),1);return uLights.lights[clamp(i32(uShadowPass.packedLightIndex),0,count-1)];}fn getShadowNormalBiasBase(light:LightUniforms)->f32{if(light.position.w>0.5){return max(light.position.y,0.0);}return max(uShadowSettings.normalBias,0.0);}fn inverseMat3(m:mat3x3<f32>)->mat3x3<f32>{let a=m[0];let b=m[1];let c=m[2];let r0=cross(b,c);let r1=cross(c,a);let r2=cross(a,b);let det=dot(a,r0);let safeDet=select(det,1.0,abs(det)<0.000001);return mat3x3<f32>(r0,r1,r2)*(1.0/safeDet);}@vertex fn vs_shadow(input:VertexInput,@builtin(instance_index)instanceIndex:u32)->@builtin(position)vec4<f32>{let realIndex=visibleIndices[instanceIndex];let inst=entityBuffer[realIndex];let matrixIndex=i32(min(uShadowPass.matrixIndex,23u));let shadowMatrix=uShadowMatrices.matrices[matrixIndex];let normalMatrix=transpose(inverseMat3(mat3x3<f32>(inst.worldMatrix[0].xyz,inst.worldMatrix[1].xyz,inst.worldMatrix[2].xyz)));let worldNormal=normalize(normalMatrix*input.normal);let worldPos4=inst.worldMatrix*vec4<f32>(input.position,1.0);let shadowLight=getShadowLight();let worldLightDir=normalize(-shadowLight.direction.xyz);let ndl=clamp(dot(worldNormal,worldLightDir),-1.0,1.0);let sinNL=sqrt(max(0.0,1.0-ndl*ndl));let directionalCasterVertScale=select(1.0,SHADOW_DIRECTIONAL_CASTER_VERT_MATCH,shadowLight.direction.w<0.5);let casterNormalBias=getShadowNormalBiasBase(shadowLight)*sinNL*directionalCasterVertScale;let biasedWorldPos4=vec4<f32>(worldPos4.xyz-worldNormal*casterNormalBias,worldPos4.w);return shadowMatrix*biasedWorldPos4;}\n"),n=a("$$SHADOW_ABI$$const SHADOW_DIRECTIONAL_CASTER_VERT_MATCH:f32=0.020;struct EntityInstance{worldMatrix:mat4x4<f32>,color:vec4<f32>,receiveShadows:f32,alphaCutoff:f32,shadowReceiverBiasMul:f32,meshId:u32,materialId:u32,};@group(1)@binding(0)var<storage,read>entityBuffer:array<EntityInstance>;@group(1)@binding(1)var<storage,read>visibleIndices:array<u32>;struct MaterialUniforms{albedo:vec4<f32>,params:vec4<f32>,advanced0:vec4<f32>,advanced1:vec4<f32>,advanced2:vec4<f32>,advanced3:vec4<f32>,advanced4:vec4<f32>,};@group(2)@binding(0)var<uniform>material:MaterialUniforms;@group(2)@binding(1)var albedoTexture:texture_2d<f32>;@group(2)@binding(7)var materialSampler:sampler;struct VertexInput{@location(0)position:vec3<f32>,@location(1)normal:vec3<f32>,@location(2)uv:vec2<f32>,};struct VertexOutput{@builtin(position)clipPos:vec4<f32>,@location(0)uv:vec2<f32>,@location(1)@interpolate(flat)alphaCutoff:f32,@location(2)@interpolate(flat)instanceTintAlpha:f32,};fn getShadowLight()->LightUniforms{let count=max(i32(uLights.count.x),1);return uLights.lights[clamp(i32(uShadowPass.packedLightIndex),0,count-1)];}fn getShadowNormalBiasBase(light:LightUniforms)->f32{if(light.position.w>0.5){return max(light.position.y,0.0);}return max(uShadowSettings.normalBias,0.0);}fn inverseMat3(m:mat3x3<f32>)->mat3x3<f32>{let a=m[0];let b=m[1];let c=m[2];let r0=cross(b,c);let r1=cross(c,a);let r2=cross(a,b);let det=dot(a,r0);let safeDet=select(det,1.0,abs(det)<0.000001);return mat3x3<f32>(r0,r1,r2)*(1.0/safeDet);}@vertex fn vs_shadow_masked(input:VertexInput,@builtin(instance_index)instanceIndex:u32)->VertexOutput{var out:VertexOutput;let realIndex=visibleIndices[instanceIndex];let inst=entityBuffer[realIndex];let matrixIndex=i32(min(uShadowPass.matrixIndex,23u));let shadowMatrix=uShadowMatrices.matrices[matrixIndex];let normalMatrix=transpose(inverseMat3(mat3x3<f32>(inst.worldMatrix[0].xyz,inst.worldMatrix[1].xyz,inst.worldMatrix[2].xyz)));let worldNormal=normalize(normalMatrix*input.normal);let worldPos4=inst.worldMatrix*vec4<f32>(input.position,1.0);let shadowLight=getShadowLight();let worldLightDir=normalize(-shadowLight.direction.xyz);let ndl=clamp(dot(worldNormal,worldLightDir),-1.0,1.0);let sinNL=sqrt(max(0.0,1.0-ndl*ndl));let directionalCasterVertScale=select(1.0,SHADOW_DIRECTIONAL_CASTER_VERT_MATCH,shadowLight.direction.w<0.5);let casterNormalBias=getShadowNormalBiasBase(shadowLight)*sinNL*directionalCasterVertScale;let biasedWorldPos4=vec4<f32>(worldPos4.xyz-worldNormal*casterNormalBias,worldPos4.w);out.clipPos=shadowMatrix*biasedWorldPos4;let uvXform=input.uv*material.advanced2.xy+material.advanced2.zw;out.uv=uvXform;out.alphaCutoff=inst.alphaCutoff;out.instanceTintAlpha=inst.color.a;return out;}@fragment fn fs_shadow_masked(in:VertexOutput){let tex=textureSample(albedoTexture,materialSampler,in.uv);var alpha=tex.a*material.albedo.a*in.instanceTintAlpha;let meshAlphaCutoff=in.alphaCutoff;let textureMaskCutoff=select(0.0,0.5,meshAlphaCutoff<=0.001&&material.albedo.a>=0.999&&tex.a<0.999);let effectiveAlphaCutoff=max(meshAlphaCutoff,textureMaskCutoff);if(effectiveAlphaCutoff>0.0&&alpha<effectiveAlphaCutoff){discard;}}\n"),s=a("$$SHADOW_ABI$$struct RigidBody{position:vec3<f32>,invMass:f32,velocity:vec3<f32>,linearDamping:f32,orientation:vec4<f32>,angularVelocity:vec3<f32>,angularDamping:f32,inverseInertia:vec3<f32>,rollingResistance:f32,restitution:f32,friction:f32,colliderID:u32,collisionFlag:u32,flags:u32,sleepCounter:u32,prevPosition:vec3<f32>,prevOrientation:vec4<f32>,collisionGroup:u32,collisionMask:u32,compliance:f32,_pad_end:f32,}struct Collider{halfExtents:vec3<f32>,shapeType:f32,}struct BatchUniform{color:vec4<f32>,receiveShadows:f32,alphaCutoff:f32,debugPhysics:f32,showStripes:f32,sphereRadiusOverride:f32,}fn quat_to_mat3(q:vec4<f32>)->mat3x3<f32>{let x=q.x;let y=q.y;let z=q.z;let w=q.w;return mat3x3<f32>(vec3<f32>(1.0-2.0*(y*y+z*z),2.0*(x*y+w*z),2.0*(x*z-w*y)),vec3<f32>(2.0*(x*y-w*z),1.0-2.0*(x*x+z*z),2.0*(y*z+w*x)),vec3<f32>(2.0*(x*z+w*y),2.0*(y*z-w*x),1.0-2.0*(x*x+y*y)));}@group(1)@binding(0)var<storage,read>bodies:array<RigidBody>;@group(1)@binding(1)var<uniform>batch:BatchUniform;@group(1)@binding(2)var<storage,read>instanceIndices:array<u32>;@group(1)@binding(3)var<storage,read>colliders:array<Collider>;struct VertexInput{@location(0)position:vec3<f32>,@location(1)normal:vec3<f32>,};@vertex fn vs_shadow(input:VertexInput,@builtin(instance_index)instanceIndex:u32)->@builtin(position)vec4<f32>{let realIndex=instanceIndices[instanceIndex];let body=bodies[realIndex];var localPos=input.position;let cid=body.colliderID;if(cid<arrayLength(&colliders)){let c=colliders[cid];if(c.shapeType<0.5||batch.sphereRadiusOverride>0.01){let r=select(max(c.halfExtents.x,0.01),batch.sphereRadiusOverride,batch.sphereRadiusOverride>0.01);let len=length(input.position);if(len>0.001){localPos=normalize(input.position)*r;}}else if(c.shapeType>=0.5&&c.shapeType<2.5){let hx=max(c.halfExtents.x,0.01);let hy=max(c.halfExtents.y,0.01);let hz=select(hx,max(c.halfExtents.z,0.01),c.halfExtents.z>0.01);localPos=localPos*vec3<f32>(2.0*hx,2.0*hy,2.0*hz);}}let R=quat_to_mat3(body.orientation);let modelMatrix=mat4x4<f32>(vec4<f32>(R[0],0.0),vec4<f32>(R[1],0.0),vec4<f32>(R[2],0.0),vec4<f32>(body.position.xyz,1.0));let worldPos4=modelMatrix*vec4<f32>(localPos,1.0);let matrixIndex=i32(min(uShadowPass.matrixIndex,23u));return uShadowMatrices.matrices[matrixIndex]*worldPos4;}\n"),c=a("$$SHADOW_ABI$$struct RigidBodyData{position:vec3f,pad0:f32,orientation:vec4f,linearVelocity:vec3f,pad1:f32,angularVelocity:vec3f,pad2:f32,invMass:f32,invInertiaX:f32,invInertiaY:f32,invInertiaZ:f32,friction:f32,rollingResistance:f32,sleepState:u32,sleepTimer:f32,dominance:u32,pad4:u32,pad5:u32,pad6:u32,}struct ColliderData{shapeType:u32,radius:f32,height:f32,pad0:u32,halfExtentsX:f32,halfExtentsY:f32,halfExtentsZ:f32,pad1:u32,}struct BatchUniform{color:vec4<f32>,receiveShadows:f32,alphaCutoff:f32,debugPhysics:f32,showStripes:f32,sphereRadiusOverride:f32,physicsAlpha:f32,perInstanceHueTint:f32,}fn quat_nlerp(a:vec4<f32>,b:vec4<f32>,t:f32)->vec4<f32>{var b_=b;if(dot(a,b)<0.0){b_=-b;}return normalize(mix(a,b_,t));}fn quat_to_mat3(q:vec4<f32>)->mat3x3<f32>{let x=q.x;let y=q.y;let z=q.z;let w=q.w;return mat3x3<f32>(vec3<f32>(1.0-2.0*(y*y+z*z),2.0*(x*y+w*z),2.0*(x*z-w*y)),vec3<f32>(2.0*(x*y-w*z),1.0-2.0*(x*x+z*z),2.0*(y*z+w*x)),vec3<f32>(2.0*(x*z+w*y),2.0*(y*z-w*x),1.0-2.0*(x*x+y*y)));}@group(1)@binding(0)var<storage,read>bodies:array<RigidBodyData>;@group(1)@binding(1)var<uniform>batch:BatchUniform;@group(1)@binding(2)var<storage,read>instanceIndices:array<u32>;@group(1)@binding(3)var<storage,read>colliders:array<ColliderData>;@group(1)@binding(4)var<storage,read>prevBodies:array<RigidBodyData>;struct VertexInput{@location(0)position:vec3<f32>,@location(1)normal:vec3<f32>,};@vertex fn vs_shadow(input:VertexInput,@builtin(instance_index)instanceIndex:u32)->@builtin(position)vec4<f32>{let realIndex=instanceIndices[instanceIndex];let body=bodies[realIndex];var pos=body.position;var orient=body.orientation;let alpha=batch.physicsAlpha;if(realIndex<arrayLength(&prevBodies)&&alpha<0.999){let prev=prevBodies[realIndex];pos=mix(prev.position,body.position,alpha);orient=quat_nlerp(prev.orientation,body.orientation,alpha);}var localPos=input.position;if(realIndex<arrayLength(&colliders)){let c=colliders[realIndex];let hx=max(c.halfExtentsX,0.01);let hy=max(c.halfExtentsY,0.01);let hz=max(c.halfExtentsZ,0.01);let r=max(c.radius,0.01);if(batch.sphereRadiusOverride>0.01){let rad=batch.sphereRadiusOverride;let len=length(input.position);if(len>0.001){localPos=normalize(input.position)*rad;}}else if(c.shapeType==0u){let len=length(input.position);if(len>0.001){localPos=normalize(input.position)*r;}}else if(c.shapeType==1u){localPos=localPos*vec3<f32>(2.0*hx,2.0*hy,2.0*hz);}else if(c.shapeType==4u){localPos=localPos*vec3<f32>(2.0*hx,2.0*hy,2.0*hz);localPos=localPos+vec3<f32>(0.0,hy*0.5,0.0);}else if(c.shapeType==2u){let len=length(input.position);if(len>0.001){localPos=normalize(input.position)*r;}}}let R=quat_to_mat3(orient);let modelMatrix=mat4x4<f32>(vec4<f32>(R[0],0.0),vec4<f32>(R[1],0.0),vec4<f32>(R[2],0.0),vec4<f32>(pos.xyz,1.0));let worldPos4=modelMatrix*vec4<f32>(localPos,1.0);let matrixIndex=i32(min(uShadowPass.matrixIndex,23u));return uShadowMatrices.matrices[matrixIndex]*worldPos4;}\n"),f=a("struct Camera{viewProjection:mat4x4<f32>,position:vec3<f32>,padding:f32,};@group(0)@binding(0)var<uniform>camera:Camera;$$SHADOW_ABI$$@group(0)@binding(4)var _envMap:texture_2d<f32>;@group(0)@binding(5)var _envSampler:sampler;@group(1)@binding(0)var _shadowMap:texture_depth_2d_array;@group(1)@binding(1)var _shadowSampler:sampler_comparison;struct MaterialUniforms{albedo:vec4<f32>,params:vec4<f32>,};@group(2)@binding(0)var<uniform>material:MaterialUniforms;@group(2)@binding(5)var emissiveTexture:texture_2d<f32>;@group(2)@binding(7)var materialSampler:sampler;struct ModelUniform{matrix:mat4x4<f32>,color:vec4<f32>,receiveShadows:f32,alphaCutoff:f32,shadowReceiverBiasMul:f32,padding:f32,};@group(3)@binding(0)var<uniform>model:ModelUniform;struct VertexInput{@location(0)position:vec3<f32>,@location(1)normal:vec3<f32>,@location(2)uv:vec2<f32>$$BLOOM_VERTEX_INPUT_EXTRA$$};struct VertexOutput{@builtin(position)position:vec4<f32>,@location(0)uv:vec2<f32>,};@vertex fn vs_main(input:VertexInput)->VertexOutput{var out:VertexOutput;let worldPos4=model.matrix*vec4<f32>(input.position,1.0);out.position=camera.viewProjection*worldPos4;out.uv=input.uv;return out;}@fragment fn fs_main(in:VertexOutput)->@location(0)vec4<f32>{let emissiveStrength=material.params.z;if(emissiveStrength<=0.0){return vec4<f32>(0.0,0.0,0.0,1.0);}let emissiveSample=textureSample(emissiveTexture,materialSampler,in.uv);let emissiveAlpha=emissiveSample.a;if(model.alphaCutoff>0.0&&emissiveAlpha<model.alphaCutoff){discard;}let emissiveColor=emissiveSample.rgb*emissiveStrength*emissiveAlpha;return vec4<f32>(emissiveColor,1.0);}\n"),d=a("struct Camera{viewProjection:mat4x4<f32>,position:vec3<f32>,padding:f32,};@group(0)@binding(0)var<uniform>camera:Camera;$$SHADOW_ABI$$@group(0)@binding(4)var _envMap:texture_2d<f32>;@group(0)@binding(5)var _envSampler:sampler;@group(1)@binding(0)var _shadowMap:texture_depth_2d_array;@group(1)@binding(1)var _shadowSampler:sampler_comparison;fn hash01_u32(x:u32)->f32{let n=f32(x)*0.1031;return fract(sin(n*12.9898+78.233)*43758.5453);}fn seedFromPosition(p:vec3<f32>)->u32{let h=sin(dot(p,vec3<f32>(12.9898,78.233,37.719)));return u32(abs(h)*4294967295.0);}fn rotateEulerXYZ(v:vec3<f32>,angles:vec3<f32>)->vec3<f32>{let sx=sin(angles.x);let cx=cos(angles.x);let sy=sin(angles.y);let cy=cos(angles.y);let sz=sin(angles.z);let cz=cos(angles.z);let rx=vec3<f32>(v.x,v.y*cx-v.z*sx,v.y*sx+v.z*cx);let ry=vec3<f32>(rx.x*cy+rx.z*sy,rx.y,-rx.x*sy+rx.z*cy);return vec3<f32>(ry.x*cz-ry.y*sz,ry.x*sz+ry.y*cz,ry.z);}struct MaterialUniforms{albedo:vec4<f32>,params:vec4<f32>,advanced0:vec4<f32>,advanced1:vec4<f32>,advanced2:vec4<f32>,advanced3:vec4<f32>,advanced4:vec4<f32>,};@group(2)@binding(0)var<uniform>material:MaterialUniforms;@group(2)@binding(5)var emissiveTexture:texture_2d<f32>;@group(2)@binding(7)var materialSampler:sampler;struct EntityInstance{worldMatrix:mat4x4<f32>,color:vec4<f32>,receiveShadows:f32,alphaCutoff:f32,shadowReceiverBiasMul:f32,meshId:u32,materialId:u32,};@group(3)@binding(0)var<storage,read>entityBuffer:array<EntityInstance>;@group(3)@binding(1)var<storage,read>visibleIndices:array<u32>;struct VertexInput{@location(0)position:vec3<f32>,@location(1)normal:vec3<f32>,@location(2)uv:vec2<f32>$$BLOOM_VERTEX_INPUT_EXTRA$$};struct VertexOutput{@builtin(position)position:vec4<f32>,@location(0)uv:vec2<f32>,@location(1)@interpolate(flat)instanceIndex:u32,};@vertex fn vs_main(input:VertexInput,@builtin(instance_index)instanceIndex:u32)->VertexOutput{var out:VertexOutput;let realIndex=visibleIndices[instanceIndex];let inst=entityBuffer[realIndex];var worldPos4:vec4<f32>;let animMode=material.advanced1.z;if(animMode>1.5){let speedScale=max(0.01,material.advanced1.w);let tW=camera.padding*speedScale;let posSeedW=seedFromPosition(inst.worldMatrix[3].xyz);let baseSeedW=posSeedW^(realIndex*747796405u+2891336453u);let phaseXW=hash01_u32(baseSeedW+0x94d049bbu)*6.28318530718;let phaseZW=hash01_u32(baseSeedW+0x7f4a7c15u)*6.28318530718;let nxW=sin(tW*1.18+phaseXW);let nzW=sin(tW*1.47+phaseZW);let h=max(input.position.y,0.0);let hnMultiplier=2.8;let hn=clamp(h*hnMultiplier,0.0,1.0);let hW=hn*hn;let maxRad=material.advanced4.w;let swayRx=nxW*maxRad*hW;let swayRz=nzW*maxRad*hW;let bentLocal=rotateEulerXYZ(input.position,vec3<f32>(swayRx,0.0,swayRz));worldPos4=inst.worldMatrix*vec4<f32>(bentLocal,1.0);}else if(animMode>0.5){let speedScale=max(0.01,material.advanced1.w);let t=camera.padding*speedScale;let posSeed=seedFromPosition(inst.worldMatrix[3].xyz);let baseSeed=posSeed^(realIndex*747796405u+2891336453u);let cell=0.13;let qp=vec3<f32>(floor(input.position.x/cell+0.5)*cell,floor(input.position.y/cell+0.5)*cell,floor(input.position.z/cell+0.5)*cell);let vSeed=baseSeed^bitcast<u32>(qp.x*173.0)^bitcast<u32>(qp.y*313.0)^bitcast<u32>(qp.z*563.0);let ph0=hash01_u32(vSeed)*6.28318530718;let ph1=hash01_u32(vSeed^0xa53c9a17u)*6.28318530718;let ph2=hash01_u32(vSeed^0x5bd1e995u)*6.28318530718;var amp=material.advanced4.w;if(amp<0.001){amp=0.02*speedScale;}amp=clamp(amp,0.004,0.28);let d=vec3<f32>(sin(t*1.21+ph0)*amp,sin(t*0.88+ph1)*amp*0.42,cos(t*1.09+ph2)*amp);let flutterLocal=input.position+d;worldPos4=inst.worldMatrix*vec4<f32>(flutterLocal,1.0);}else{worldPos4=inst.worldMatrix*vec4<f32>(input.position,1.0);}out.position=camera.viewProjection*worldPos4;out.uv=input.uv;out.instanceIndex=realIndex;return out;}@fragment fn fs_main(in:VertexOutput)->@location(0)vec4<f32>{let emissiveStrength=material.params.z;if(emissiveStrength<=0.0){return vec4<f32>(0.0,0.0,0.0,1.0);}let inst=entityBuffer[in.instanceIndex];let emissiveSample=textureSample(emissiveTexture,materialSampler,in.uv);let emissiveAlpha=emissiveSample.a;if(inst.alphaCutoff>0.0&&emissiveAlpha<inst.alphaCutoff){discard;}let emissiveColor=emissiveSample.rgb*emissiveStrength*emissiveAlpha;return vec4<f32>(emissiveColor,1.0);}\n");function u(e,t){const a=!!t?.enableVelocityOutput,r=a?"1":"0",o=a?"struct FragmentOutput {\n    @location(0) color: vec4<f32>,\n    @location(1) velocity: vec2<f32>,\n};":"",l=a?"FragmentOutput":"@location(0) vec4<f32>",i=a?"let worldPos4Vel = vec4<f32>(in.worldPos, 1.0);\n    let curClipVel = camera.viewProjection * worldPos4Vel;\n    let prevClipVel = camera.prevViewProjection * worldPos4Vel;\n    let curNDC = curClipVel.xy / curClipVel.w;\n    let prevNDC = prevClipVel.xy / prevClipVel.w;\n    let velocity = vec2<f32>((curNDC.x - prevNDC.x) * 0.5, (prevNDC.y - curNDC.y) * 0.5);":"",n=a?"return FragmentOutput(outColor, velocity);":"return outColor;";return e.replace(/\$\$ENABLE_VELOCITY_OUTPUT\$\$/g,r).replace(/\$\$VELOCITY_FRAGMENT_STRUCT\$\$/g,o).replace(/\$\$FRAGMENT_RETURN_TYPE\$\$/g,l).replace(/\$\$VELOCITY_CALC\$\$/g,i).replace(/\$\$FRAGMENT_RETURN\$\$/g,n)}function m(e){return e.replace(/\$\$MATERIAL_PARALLAX_WGSL\$\$/g,"fn materialBaseUv(uv:vec2<f32>)->vec2<f32>{return uv*material.advanced2.xy+material.advanced2.zw;}fn buildTbn(N:vec3<f32>,tangentOrZero:vec3<f32>,tangentHandedness:f32)->mat3x3<f32>{var T=tangentOrZero;if(length(T)<=0.001){let up=select(vec3<f32>(0.0,1.0,0.0),vec3<f32>(1.0,0.0,0.0),abs(N.y)>0.99);T=normalize(cross(up,N));}else{T=normalize(T);}let B=normalize(cross(N,T))*select(-1.0,1.0,tangentHandedness>=0.0);return mat3x3<f32>(T,B,N);}fn sampleHeightPom(uv:vec2<f32>)->f32{return 1.0-textureSampleLevel(heightTexture,materialSampler,uv,0.0).r;}fn materialHash21(p:vec2<f32>)->f32{let q=fract(vec2<f32>(dot(p,vec2<f32>(127.1,311.7)),dot(p,vec2<f32>(269.5,183.3))));return fract(sin(q.x+q.y)*43758.5453);}fn materialValueNoise(p:vec2<f32>)->f32{let i=floor(p);let f=fract(p);let u=f*f*(vec2<f32>(3.0)-2.0*f);let a=materialHash21(i);let b=materialHash21(i+vec2<f32>(1.0,0.0));let c=materialHash21(i+vec2<f32>(0.0,1.0));let d=materialHash21(i+vec2<f32>(1.0,1.0));return mix(mix(a,b,u.x),mix(c,d,u.x),u.y);}fn materialMacroVariation(worldPos:vec3<f32>,N:vec3<f32>,parallaxMode:f32)->f32{if(parallaxMode<0.5){return 1.0;}let wallU=select(worldPos.z,worldPos.x,abs(N.z)>abs(N.x));let coord=vec2<f32>(wallU,worldPos.y)*0.33;let broad=materialValueNoise(coord);let fine=materialValueNoise(coord*2.7+vec2<f32>(13.1,7.7));return mix(0.96,1.04,broad*0.72+fine*0.28);}fn parallaxOcclusionMapping(uv:vec2<f32>,viewDir:vec3<f32>,N:vec3<f32>,heightScale:f32,tangent:vec3<f32>,tangentHandedness:f32,parallaxMode:f32,minStepsRaw:f32,maxStepsRaw:f32,refineStepsRaw:f32)->vec2<f32>{if(parallaxMode<0.5||heightScale<=0.0001||length(tangent)<=0.001){return uv;}let tbn=buildTbn(N,tangent,tangentHandedness);let viewDirTS=normalize(vec3<f32>(dot(viewDir,tbn[0]),dot(viewDir,tbn[1]),dot(viewDir,tbn[2])));if(viewDirTS.z<=0.05){return uv;}let angle=clamp(abs(viewDirTS.z),0.0,1.0);let angleFade=smoothstep(0.05,0.25,angle);let effectiveHeightScale=heightScale*angleFade;if(effectiveHeightScale<=0.0001){return uv;}let minSteps=clamp(floor(minStepsRaw),1.0,96.0);let maxSteps=clamp(floor(maxStepsRaw),minSteps,96.0);let numLayersF=floor(mix(maxSteps,minSteps,angle));let numLayers=u32(numLayersF);let layerDepth=1.0/max(numLayersF,1.0);let parallaxLimit=clamp(effectiveHeightScale,0.0,0.07);let viewSlope=viewDirTS.xy/max(viewDirTS.z,0.18);let deltaUV=viewSlope*(parallaxLimit/max(numLayersF,1.0));var currentUV=uv;var currentLayerDepth=0.0;var currentHeight=sampleHeightPom(currentUV);var previousUV=currentUV;var previousLayerDepth=currentLayerDepth;var previousHeight=currentHeight;for(var i=0u;i<96u;i++){if(i>=numLayers||currentLayerDepth>=currentHeight){break;}previousUV=currentUV;previousLayerDepth=currentLayerDepth;previousHeight=currentHeight;currentUV=currentUV-deltaUV;currentLayerDepth=currentLayerDepth+layerDepth;currentHeight=sampleHeightPom(currentUV);}let afterDepth=currentHeight-currentLayerDepth;let beforeDepth=previousHeight-previousLayerDepth;let denom=max((afterDepth-beforeDepth),0.00001);let weight=clamp(afterDepth/denom,0.0,1.0);var refinedUV=mix(currentUV,previousUV,weight);let refineSteps=u32(clamp(floor(refineStepsRaw),0.0,8.0));var lo=currentUV;var hi=previousUV;for(var r=0u;r<8u;r++){if(r>=refineSteps){break;}let mid=(lo+hi)*0.5;let midHeight=sampleHeightPom(mid);let midLayerDepth=mix(currentLayerDepth,previousLayerDepth,0.5);if(midLayerDepth>=midHeight){lo=mid;}else{hi=mid;}refinedUV=(lo+hi)*0.5;}if(any(abs(refinedUV)>vec2<f32>(1048576.0))){return uv;}return refinedUV;}fn parallaxSelfShadow(uv:vec2<f32>,lightDir:vec3<f32>,N:vec3<f32>,heightScale:f32,tangent:vec3<f32>,tangentHandedness:f32,parallaxMode:f32)->f32{if(parallaxMode<0.5||heightScale<=0.0001||length(tangent)<=0.001){return 1.0;}let tbn=buildTbn(N,tangent,tangentHandedness);let lightDirTS=normalize(vec3<f32>(dot(lightDir,tbn[0]),dot(lightDir,tbn[1]),dot(lightDir,tbn[2])));if(lightDirTS.z<=0.08){return 0.94;}let steps=8u;let currentHeight=sampleHeightPom(uv);let slope=lightDirTS.xy/max(lightDirTS.z,0.22);let shadowReach=clamp(heightScale,0.0,0.08)*0.58;var occlusion=0.0;for(var i=1u;i<=8u;i++){if(i>steps){break;}let t=f32(i)/f32(steps);let sampleUv=uv+slope*shadowReach*t;let rayHeight=currentHeight+(1.0-currentHeight)*t;let sampleHeight=sampleHeightPom(sampleUv);let hit=select(0.0,1.0,sampleHeight>rayHeight+0.055);occlusion=max(occlusion,hit*(1.0-t*0.70));}let angleFade=smoothstep(0.18,0.72,lightDirTS.z);let strength=clamp(heightScale*7.0,0.0,0.20)*angleFade;return mix(1.0,1.0-strength,occlusion);}\n")}function p(e){return e?.enableVelocityOutput?"_vel":""}var h=(e=>(e.STANDARD="standard",e.UNLIT="unlit",e.LIT="lit",e.EMISSIVE="emissive",e.BLOOM_EXTRACT="bloom_extract",e))(h||{});const v="struct Camera{viewProjection:mat4x4<f32>,position:vec3<f32>,padding:f32,prevViewProjection:mat4x4<f32>,};@group(0)@binding(0)var<uniform>camera:Camera;$$LIGHTING_COMMON$$@group(0)@binding(4)var envMap:texture_2d<f32>;fn sampleEquirectangular(dir:vec3<f32>)->vec3<f32>{let n=normalize(dir);let lon=atan2(n.z,n.x);let lat=asin(clamp(n.y,-1.0,1.0));let u=fract(lon/6.28318530718+0.5);let v=0.5-(lat/3.14159265359);return textureSample(envMap,iblSampler,vec2<f32>(u,clamp(v,0.0,1.0))).rgb;}struct MaterialUniforms{albedo:vec4<f32>,params:vec4<f32>,advanced0:vec4<f32>,advanced1:vec4<f32>,advanced2:vec4<f32>,advanced3:vec4<f32>,advanced4:vec4<f32>,};@group(2)@binding(0)var<uniform>material:MaterialUniforms;@group(2)@binding(1)var albedoTexture:texture_2d<f32>;@group(2)@binding(2)var normalTexture:texture_2d<f32>;@group(2)@binding(3)var metallicRoughnessTexture:texture_2d<f32>;@group(2)@binding(4)var aoTexture:texture_2d<f32>;@group(2)@binding(5)var emissiveTexture:texture_2d<f32>;@group(2)@binding(6)var heightTexture:texture_2d<f32>;@group(2)@binding(7)var materialSampler:sampler;@group(2)@binding(8)var reflectionTexture:texture_2d<f32>;@group(2)@binding(9)var reflectionSampler:sampler;struct ReflectionCameraData{matrix:mat4x4<f32>}@group(2)@binding(10)var<uniform>reflectionCamera:ReflectionCameraData;struct ModelUniform{matrix:mat4x4<f32>,color:vec4<f32>,receiveShadows:f32,alphaCutoff:f32,shadowReceiverBiasMul:f32,surfaceGridEnable:f32,};@group(3)@binding(0)var<uniform>model:ModelUniform;struct VertexInput{@location(0)position:vec3<f32>,@location(1)normal:vec3<f32>,@location(2)uv:vec2<f32>$$VERTEX_INPUT_EXTRA$$};struct VertexOutput{@builtin(position)position:vec4<f32>,@location(0)worldPos:vec3<f32>,@location(1)normal:vec3<f32>,@location(2)uv:vec2<f32>$$VERTEX_OUTPUT_EXTRA$$};const GLOBAL_SPECULAR_DAMP:f32=0.28;@vertex fn vs_main(input:VertexInput)->VertexOutput{var out:VertexOutput;let worldPos4=model.matrix*vec4<f32>(input.position,1.0);out.position=camera.viewProjection*worldPos4;out.worldPos=worldPos4.xyz;let rotMat=mat3x3<f32>(model.matrix[0].xyz,model.matrix[1].xyz,model.matrix[2].xyz);out.normal=normalize(rotMat*input.normal);out.uv=input.uv;$$VS_MAIN_EXTRA$$return out;}fn fresnelSchlick(cosTheta:f32,F0:vec3<f32>)->vec3<f32>{return F0+(vec3<f32>(1.0)-F0)*pow(clamp(1.0-cosTheta,0.0,1.0),5.0);}fn distributionGGX(NdotH:f32,roughness:f32)->f32{let a=roughness*roughness;let a2=a*a;let NdotH2=NdotH*NdotH;return(a2)/max(3.14159265359*(NdotH2*(a2-1.0)+1.0)*(NdotH2*(a2-1.0)+1.0),0.0000001);}fn geometrySchlickGGX(NdotV:f32,roughness:f32)->f32{let r=(roughness+1.0);let k=(r*r)/8.0;return NdotV/max(NdotV*(1.0-k)+k,0.0000001);}fn geometrySmith(NdotV:f32,NdotL:f32,roughness:f32)->f32{return geometrySchlickGGX(NdotV,roughness)*geometrySchlickGGX(NdotL,roughness);}fn dielectricF0FromIOR(ior:f32)->f32{let i=clamp(ior,1.0,2.5);let f=(i-1.0)/(i+1.0);return f*f;}fn resolveTangentOrFallback(N:vec3<f32>,tangentOrZero:vec3<f32>)->vec3<f32>{if(length(tangentOrZero)>0.001){return normalize(tangentOrZero);}let up=select(vec3<f32>(0.0,1.0,0.0),vec3<f32>(1.0,0.0,0.0),abs(N.y)>0.99);return normalize(cross(up,N));}fn anisotropicRoughness(roughness:f32,anisotropy:f32,anisotropyRotation:f32,N:vec3<f32>,H:vec3<f32>,tangentOrZero:vec3<f32>)->f32{let a=clamp(anisotropy,-1.0,1.0);if(abs(a)<0.001){return clamp(roughness,0.02,1.0);}if(length(tangentOrZero)<=0.001){return clamp(roughness,0.02,1.0);}let T=resolveTangentOrFallback(N,tangentOrZero);let B=normalize(cross(N,T));let angle=clamp(anisotropyRotation,0.0,1.0)*6.28318530718;let Trot=normalize(T*cos(angle)+B*sin(angle));let hProj=H-N*dot(H,N);let hProjLen=length(hProj);if(hProjLen<0.0001){return clamp(roughness,0.02,1.0);}let hTan=hProj/hProjLen;let aligned=abs(dot(hTan,Trot));let rMin=max(0.02,roughness*(1.0-abs(a)*0.7));let rMax=min(1.0,roughness*(1.0+abs(a)*0.7));let blend=select(1.0-aligned,aligned,a>=0.0);return clamp(mix(rMin,rMax,blend),0.02,1.0);}fn envBRDFApprox(F0:vec3<f32>,roughness:f32,NdotV:f32)->vec3<f32>{let c0=vec4<f32>(-1.0,-0.0275,-0.572,0.022);let c1=vec4<f32>(1.0,0.0425,1.04,-0.04);let r=roughness*c0+c1;let a004=min(r.x*r.x,exp2(-9.28*NdotV))*r.x+r.y;let ab=vec2<f32>(-1.04,1.04)*a004+r.zw;return F0*ab.x+vec3<f32>(ab.y);}$$MATERIAL_PARALLAX_WGSL$$fn gridLineMask(worldXZ:vec2<f32>,stepMeters:f32,halfThicknessMeters:f32)->f32{let gx=abs(fract(worldXZ.x/stepMeters+0.5)-0.5)*stepMeters;let gz=abs(fract(worldXZ.y/stepMeters+0.5)-0.5)*stepMeters;let lineDist=min(gx,gz);return 1.0-smoothstep(halfThicknessMeters,halfThicknessMeters+0.012,lineDist);}fn terrainGridLineMask(uv:vec2<f32>,resolution:f32,stepCells:f32,halfThicknessCells:f32)->f32{let cell=uv*resolution;let gx=abs(fract(cell.x/stepCells+0.5)-0.5)*stepCells;let gz=abs(fract(cell.y/stepCells+0.5)-0.5)*stepCells;let lineDist=min(gx,gz);let aa=max(max(fwidth(cell.x),fwidth(cell.y))*1.35,0.006);return 1.0-smoothstep(halfThicknessCells,halfThicknessCells+aa,lineDist);}fn cookTorranceBRDF(N:vec3<f32>,V:vec3<f32>,L:vec3<f32>,albedo:vec3<f32>,metallic:f32,roughness:f32,F0:vec3<f32>,transmission:f32)->vec3<f32>{let wrap=0.0;let H=normalize(V+L);let NdotV=max(dot(N,V),0.0000001);let NdotL=max(dot(N,L),0.0000001);let wrappedNdotL=max((dot(N,L)+wrap)/(1.0+wrap),0.0);let NdotH=max(dot(N,H),0.0);let VdotH=max(dot(V,H),0.0);let F=fresnelSchlick(VdotH,F0);let D=distributionGGX(NdotH,roughness);let G=geometrySmith(NdotV,NdotL,roughness);let numerator=D*G*F;let denominator=4.0*NdotV*NdotL;let specular=min(numerator/max(denominator,0.0000001),vec3<f32>(4.0));let kD=(vec3<f32>(1.0)-F)*(1.0-metallic)*(1.0-transmission);let diffuseTerm=(kD*albedo/3.14159265359)*wrappedNdotL;let specularTerm=specular*NdotL*GLOBAL_SPECULAR_DAMP;return diffuseTerm+specularTerm;}fn clearcoatSpecular(N:vec3<f32>,V:vec3<f32>,L:vec3<f32>,clearcoatRoughness:f32)->vec3<f32>{let H=normalize(V+L);let NdotV=max(dot(N,V),0.0000001);let NdotL=max(dot(N,L),0.0000001);let NdotH=max(dot(N,H),0.0);let VdotH=max(dot(V,H),0.0);let F=fresnelSchlick(VdotH,vec3<f32>(0.04));let D=distributionGGX(NdotH,clearcoatRoughness);let G=geometrySmith(NdotV,NdotL,clearcoatRoughness);let spec=(D*G*F)/max(4.0*NdotV*NdotL,0.0000001);return spec*NdotL*GLOBAL_SPECULAR_DAMP;}$$VELOCITY_FRAGMENT_STRUCT$$@fragment fn fs_main(in:VertexOutput,@builtin(front_facing)front_facing:bool)->$$FRAGMENT_RETURN_TYPE$${let N_geom=select(-normalize(in.normal),normalize(in.normal),front_facing);var N=N_geom;let distToCamera=length(camera.position-in.worldPos);let V=(camera.position-in.worldPos)/distToCamera;var baseUv=materialBaseUv(in.uv);var surfaceTangent=$$PARALLAX_TANGENT$$;var surfaceTangentW=$$PARALLAX_TANGENT_W$$;if(model.surfaceGridEnable>1.5){let nAbs=abs(N_geom);var projectedUv=in.worldPos.xz;let worldUvScale=0.55;if(model.surfaceGridEnable>2.5){let downXZRaw=vec2<f32>(N_geom.x,N_geom.z);let downXZLen=max(length(downXZRaw),1e-4);let downXZ=downXZRaw/downXZLen;let ridgeXZ=vec2<f32>(-downXZ.y,downXZ.x);projectedUv=vec2<f32>(dot(in.worldPos.xz,ridgeXZ),dot(in.worldPos.xz,downXZ));surfaceTangent=normalize(vec3<f32>(ridgeXZ.x,0.0,ridgeXZ.y));surfaceTangentW=1.0;}else if(nAbs.y>=nAbs.x&&nAbs.y>=nAbs.z){projectedUv=in.worldPos.xz;surfaceTangent=vec3<f32>(1.0,0.0,0.0);surfaceTangentW=1.0;}else if(nAbs.x>=nAbs.z){projectedUv=vec2<f32>(in.worldPos.z,in.worldPos.y);surfaceTangent=vec3<f32>(0.0,0.0,1.0);surfaceTangentW=1.0;}else{projectedUv=vec2<f32>(in.worldPos.x,in.worldPos.y);surfaceTangent=vec3<f32>(1.0,0.0,0.0);surfaceTangentW=1.0;}baseUv=materialBaseUv(projectedUv*worldUvScale);}var uv=baseUv;if(distToCamera<60.0){uv=parallaxOcclusionMapping(baseUv,V,N,material.params.w,surfaceTangent,surfaceTangentW,material.advanced3.z,material.advanced4.x,material.advanced4.y,material.advanced4.z);}let albedoTexColor=textureSample(albedoTexture,materialSampler,uv);var baseColor=albedoTexColor.rgb*material.albedo.rgb*model.color.rgb$$VERTEX_COLOR_MUL$$;baseColor*=materialMacroVariation(in.worldPos,N_geom,material.advanced3.z);var alpha=albedoTexColor.a*material.albedo.a$$VERTEX_COLOR_ALPHA$$;let meshAlphaCutoff=model.alphaCutoff;let textureMaskCutoff=select(0.0,0.5,meshAlphaCutoff<=0.001&&material.albedo.a>=0.999&&albedoTexColor.a<0.999);let effectiveAlphaCutoff=max(meshAlphaCutoff,textureMaskCutoff);let alphaMaskedOpaque=effectiveAlphaCutoff>0.0&&material.albedo.a>=0.999;if(effectiveAlphaCutoff>0.0){if(alpha<effectiveAlphaCutoff){discard;}alpha=1.0;}let mrSample=textureSample(metallicRoughnessTexture,materialSampler,uv);let finalMetallic=material.params.x*mrSample.b;let finalRoughness=clamp(material.params.y*mrSample.g,0.03,1.0);let clearcoat=clamp(material.advanced0.x,0.0,1.0);let clearcoatRoughness=clamp(material.advanced0.y,0.03,1.0);let transmission=clamp(material.advanced0.z,0.0,1.0)*(1.0-finalMetallic);let ior=clamp(material.advanced0.w,1.0,2.5);let anisotropy=clamp(material.advanced1.x,-1.0,1.0);let anisotropyRotation=clamp(material.advanced1.y,0.0,1.0);let dielectricF0=dielectricF0FromIOR(ior);let baseF0=mix(vec3<f32>(dielectricF0),baseColor,finalMetallic);let normalMapSample=textureSample(normalTexture,materialSampler,uv);let tangentOrZero=surfaceTangent;if(length(tangentOrZero)>0.001&&distToCamera<180.0){var normalMapTS=normalMapSample.rgb*2.0-vec3<f32>(1.0);let normalStrength=material.advanced3.x;normalMapTS=normalize(vec3<f32>(normalMapTS.xy*normalStrength,normalMapTS.z));let T=normalize(tangentOrZero);let B=cross(N,T)*select(-1.0,1.0,surfaceTangentW>=0.0);let TBN=mat3x3<f32>(T,B,N);N=normalize(TBN*normalMapTS);}let concreteLike=select(0.0,1.0,abs(baseColor.r-baseColor.g)<0.06&&abs(baseColor.g-baseColor.b)<0.06)*select(0.0,1.0,finalMetallic<0.2&&finalRoughness>0.4);let upwardLitGate=smoothstep(0.0,0.35,N.y);let litFlatten=0.58*concreteLike*upwardLitGate;let N_lit=normalize(mix(N,vec3<f32>(0.0,1.0,0.0),litFlatten));let shadowReceiveGate=1.0;let roofUndersideGeomMul=mix(0.40,1.0,smoothstep(-0.95,-0.45,N_geom.y));let backfaceShadowMul=select(0.65,1.0,front_facing);let shadowRecvForMap=model.receiveShadows*shadowReceiveGate*roofUndersideGeomMul*backfaceShadowMul;let receiverShadowBiasMul=clamp(model.shadowReceiverBiasMul,0.5,4.0);var directLighting=vec3<f32>(0.0);let lightCount=getLightCount();for(var i=0;i<MAX_LIGHTS;i++){if(i>=lightCount){break;}let light=getLightAt(i);let lightData=getLightDirectionAndDistance(light,in.worldPos);let L=lightData.xyz;let lightDistance=lightData.w;let geomSunGate=select(1.0,directionalSunShadowReceiveGate(L,N_geom),light.direction.w<0.5);let recvShadows=shadowRecvForMap*geomSunGate;let applyShadow=shouldApplyShadowForLight(i,light.direction.w);let pomSelfShadow=select(1.0,parallaxSelfShadow(uv,L,N_geom,material.params.w,tangentOrZero,surfaceTangentW,material.advanced3.z),distToCamera<60.0);let radiance=getLightRadiance(i,light,in.worldPos,N_geom,recvShadows,L,lightDistance,applyShadow,receiverShadowBiasMul,)*2.0*pomSelfShadow;let H=normalize(V+L);let anisoRoughness=anisotropicRoughness(finalRoughness,anisotropy,anisotropyRotation,N_lit,H,tangentOrZero);directLighting+=cookTorranceBRDF(N_lit,V,L,baseColor,finalMetallic,anisoRoughness,baseF0,transmission)*radiance;directLighting+=clearcoatSpecular(N_lit,V,L,clearcoatRoughness)*radiance*(clearcoat*0.25);}let R=reflect(-V,N_lit);let refractDir=refract(-V,N_lit,1.0/ior);let NdotV=max(dot(N_lit,V),0.0);let F_env=fresnelSchlick(NdotV,baseF0);let reflectionStrength=(1.0-finalRoughness)*max(finalMetallic+dielectricF0*(1.0-finalMetallic),0.04);let planarWeight=clamp((1.0-finalRoughness)*(0.25+finalMetallic*0.75+clearcoat*0.45),0.0,1.0);let isFloorLike=abs(in.worldPos.y)<=0.12&&N_geom.y>=0.7;let worldPos4=vec4<f32>(in.worldPos,1.0);let reflectionClip=reflectionCamera.matrix*worldPos4;let reflectionW=max(abs(reflectionClip.w),0.000001);let reflectionNDC=reflectionClip.xyz/reflectionW;let reflectionInFrustum=abs(reflectionNDC.x)<=1.0&&abs(reflectionNDC.y)<=1.0&&reflectionNDC.z>=-1.0&&reflectionNDC.z<=1.0;var reflectionUV=clamp(vec2<f32>(reflectionNDC.x*0.5+0.5,-reflectionNDC.y*0.5+0.5),vec2<f32>(0.0),vec2<f32>(1.0));let reflectionColor=textureSample(reflectionTexture,reflectionSampler,reflectionUV);let specularIBL=getSpecularIBL(R,finalRoughness,NdotV,baseF0);let planarEnable=reflectionInFrustum&&isFloorLike&&planarWeight>0.2;let reflectionBase=select(specularIBL,reflectionColor.rgb,planarEnable);let iblIntensity=getIblIntensity();let topDownAtt=getTopDownSpecularAttenuation(V);var reflection=reflectionBase*reflectionStrength*iblIntensity*topDownAtt*GLOBAL_SPECULAR_DAMP;let clearcoatSpecIBL=getSpecularIBL(R,clearcoatRoughness,NdotV,vec3<f32>(0.04));var clearcoatReflection=clearcoatSpecIBL*(clearcoat*0.35)*iblIntensity*topDownAtt*GLOBAL_SPECULAR_DAMP;let ao=pow(max(textureSample(aoTexture,materialSampler,uv).r,0.001),clamp(material.advanced3.y,0.0,4.0));let ambientStrength=getAmbientStrength();let ambientColor=getAmbientColor();let kD=(vec3<f32>(1.0)-F_env)*(1.0-finalMetallic)*(1.0-transmission);let ambientNormal=N_geom;let diffuseIBL=getDiffuseIndirect(ambientNormal,in.worldPos);var diffuseAmbient=kD*baseColor*(diffuseIBL*(0.5*iblIntensity)+ambientStrength*0.25+ambientColor*uShadowSettings.ambientIntensity)*ao;let hemi=mix(1.22,1.04,clamp(ambientNormal.y*0.5+0.5,0.0,1.0));let undersideLift=smoothstep(0.0,0.65,-ambientNormal.y);diffuseAmbient*=hemi*mix(1.0,1.0,undersideLift);let transmissionEnv=getDiffuseIndirect(refractDir,in.worldPos);let transmissionTerm=transmissionEnv*baseColor*transmission*(1.0-finalRoughness*0.75)*ao*iblIntensity;let metalAmbientLift=getDiffuseIndirect(ambientNormal,in.worldPos)*baseColor*finalMetallic*(0.12+ambientStrength*0.18)*ao*iblIntensity;let ambient=diffuseAmbient+reflection+clearcoatReflection+transmissionTerm+metalAmbientLift;let emissiveColor=textureSample(emissiveTexture,materialSampler,uv).rgb*material.params.z;var finalColor=directLighting+ambient+emissiveColor;let foliageLightFloor=select(0.0,0.18,alphaMaskedOpaque);finalColor=max(finalColor,baseColor*foliageLightFloor);let terrainCellStepMeters=max(0.001,material.advanced4.z);let terrainChunkStepCells=max(1.0,round(material.advanced4.x));let terrainRegionStepCells=max(terrainChunkStepCells,round(material.advanced4.y));let terrainChunkStepMeters=terrainCellStepMeters*terrainChunkStepCells;let terrainRegionStepMeters=terrainCellStepMeters*terrainRegionStepCells;let terrainRegionMask=gridLineMask(in.worldPos.xz,terrainRegionStepMeters,max(0.06,terrainCellStepMeters*0.06));let terrainChunkRawMask=gridLineMask(in.worldPos.xz,terrainChunkStepMeters,max(0.045,terrainCellStepMeters*0.045));let terrainCellRawMask=gridLineMask(in.worldPos.xz,terrainCellStepMeters,max(0.022,terrainCellStepMeters*0.022));if(model.surfaceGridEnable>0.5&&model.surfaceGridEnable<1.5){let major5m=gridLineMask(in.worldPos.xz,5.0,0.050);let minor1mRaw=gridLineMask(in.worldPos.xz,1.0,0.016);let minor1m=clamp(minor1mRaw-major5m*0.9,0.0,1.0);let minorColor=vec3<f32>(0.40,0.44,0.50);let majorColor=vec3<f32>(0.78,0.81,0.86);finalColor=mix(finalColor,minorColor,minor1m*0.55);finalColor=mix(finalColor,majorColor,major5m*0.88);}else if(model.surfaceGridEnable<-0.5){let gridModeEncoded=u32(max(0.0,round(abs(model.surfaceGridEnable))));let gridMask=select(0u,gridModeEncoded-1u,gridModeEncoded>=1u);let showCell=(gridMask&1u)!=0u;let showChunk=(gridMask&2u)!=0u;let showRegion=(gridMask&4u)!=0u;let showCollision=(gridMask&8u)!=0u;let region=terrainRegionMask;let chunk=clamp(terrainChunkRawMask-region*0.95,0.0,1.0);let cell=clamp(terrainCellRawMask-max(chunk,region)*0.95,0.0,1.0);let collision=clamp(terrainCellRawMask-max(chunk,region)*0.95,0.0,1.0);let cellColor=vec3<f32>(0.92,0.96,1.0);let collisionColor=vec3<f32>(0.05,0.82,0.95);let chunkColor=vec3<f32>(1.0,0.55,0.10);let regionColor=vec3<f32>(1.0,0.08,0.08);finalColor=mix(finalColor,cellColor,select(0.0,cell*0.24,showCell));finalColor=mix(finalColor,collisionColor,select(0.0,collision*0.32,showCollision));finalColor=mix(finalColor,chunkColor,select(0.0,chunk*0.76,showChunk));finalColor=mix(finalColor,regionColor,select(0.0,region*0.94,showRegion));}let debugView=u32(material.advanced3.w);if(debugView==1u){finalColor=baseColor;}if(debugView==2u){finalColor=normalMapSample.rgb;}if(debugView==3u){finalColor=vec3<f32>(finalRoughness);}if(debugView==4u){finalColor=vec3<f32>(finalMetallic);}if(debugView==5u){finalColor=vec3<f32>(ao);}if(debugView==6u){finalColor=vec3<f32>(textureSample(heightTexture,materialSampler,uv).r);}if(debugView==7u){finalColor=vec3<f32>(fract(uv),0.0);}if(debugView==8u){finalColor=abs(normalize(N))*0.85+vec3<f32>(0.075);}finalColor=applyFog(finalColor,in.worldPos,camera.position);let outColor=vec4<f32>(finalColor,alpha);$$VELOCITY_CALC$$$$FRAGMENT_RETURN$$}\n".replace(/\$\$LIGHTING_COMMON\$\$/g,o),g="struct Camera{viewProjection:mat4x4<f32>,position:vec3<f32>,padding:f32,prevViewProjection:mat4x4<f32>,};@group(0)@binding(0)var<uniform>camera:Camera;$$LIGHTING_COMMON$$@group(0)@binding(4)var envMap:texture_2d<f32>;fn sampleEquirectangular(dir:vec3<f32>)->vec3<f32>{let n=normalize(dir);let lon=atan2(n.z,n.x);let lat=asin(clamp(n.y,-1.0,1.0));let u=fract(lon/6.28318530718+0.5);let v=0.5-(lat/3.14159265359);return textureSample(envMap,iblSampler,vec2<f32>(u,clamp(v,0.0,1.0))).rgb;}fn hash01_u32(x:u32)->f32{let n=f32(x)*0.1031;return fract(sin(n*12.9898+78.233)*43758.5453);}fn seedFromPosition(p:vec3<f32>)->u32{let h=sin(dot(p,vec3<f32>(12.9898,78.233,37.719)));return u32(abs(h)*4294967295.0);}fn rotateAroundAxis(v:vec3<f32>,axisRaw:vec3<f32>,angle:f32)->vec3<f32>{let axis=normalize(axisRaw+vec3<f32>(0.0001,0.0,0.0));let s=sin(angle);let c=cos(angle);return v*c+cross(axis,v)*s+axis*dot(axis,v)*(1.0-c);}fn rotateEulerXYZ(v:vec3<f32>,angles:vec3<f32>)->vec3<f32>{let sx=sin(angles.x);let cx=cos(angles.x);let sy=sin(angles.y);let cy=cos(angles.y);let sz=sin(angles.z);let cz=cos(angles.z);let rx=vec3<f32>(v.x,v.y*cx-v.z*sx,v.y*sx+v.z*cx);let ry=vec3<f32>(rx.x*cy+rx.z*sy,rx.y,-rx.x*sy+rx.z*cy);return vec3<f32>(ry.x*cz-ry.y*sz,ry.x*sz+ry.y*cz,ry.z);}struct MaterialUniforms{albedo:vec4<f32>,params:vec4<f32>,advanced0:vec4<f32>,advanced1:vec4<f32>,advanced2:vec4<f32>,advanced3:vec4<f32>,advanced4:vec4<f32>,brushOverlay:vec4<f32>,};@group(2)@binding(0)var<uniform>material:MaterialUniforms;@group(2)@binding(1)var albedoTexture:texture_2d<f32>;@group(2)@binding(2)var normalTexture:texture_2d<f32>;@group(2)@binding(3)var metallicRoughnessTexture:texture_2d<f32>;@group(2)@binding(4)var aoTexture:texture_2d<f32>;@group(2)@binding(5)var emissiveTexture:texture_2d<f32>;@group(2)@binding(6)var heightTexture:texture_2d<f32>;@group(2)@binding(7)var materialSampler:sampler;@group(2)@binding(8)var reflectionTexture:texture_2d<f32>;@group(2)@binding(9)var reflectionSampler:sampler;struct ReflectionCameraData{matrix:mat4x4<f32>}@group(2)@binding(10)var<uniform>reflectionCamera:ReflectionCameraData;struct EntityInstance{worldMatrix:mat4x4<f32>,color:vec4<f32>,receiveShadows:f32,alphaCutoff:f32,shadowReceiverBiasMul:f32,surfaceGridEnable:f32,meshId:u32,materialId:u32,};@group(3)@binding(0)var<storage,read>entityBuffer:array<EntityInstance>;@group(3)@binding(1)var<storage,read>visibleIndices:array<u32>;struct VertexInput{@location(0)position:vec3<f32>,@location(1)normal:vec3<f32>,@location(2)uv:vec2<f32>$$VERTEX_INPUT_EXTRA$$};struct VertexOutput{@builtin(position)position:vec4<f32>,@location(0)worldPos:vec3<f32>,@location(1)normal:vec3<f32>,@location(2)uv:vec2<f32>$$VERTEX_OUTPUT_EXTRA$$,@location(5)@interpolate(flat)instanceIndex:u32};const GLOBAL_SPECULAR_DAMP:f32=0.28;@vertex fn vs_main(input:VertexInput,@builtin(instance_index)instanceIndex:u32)->VertexOutput{var out:VertexOutput;let realIndex=visibleIndices[instanceIndex];let inst=entityBuffer[realIndex];var worldPos4:vec4<f32>;$$INSTANCED_TAN_DECL$$let animMode=material.advanced1.z;if(animMode>1.5){let speedScale=max(0.01,material.advanced1.w);let tW=camera.padding*speedScale;let posSeedW=seedFromPosition(inst.worldMatrix[3].xyz);let baseSeedW=posSeedW^(realIndex*747796405u+2891336453u);let phaseGust=hash01_u32(baseSeedW+0x94d049bbu)*6.28318530718;let phaseFlutter=hash01_u32(baseSeedW+0x7f4a7c15u)*6.28318530718;let phaseMacro=hash01_u32(baseSeedW+0x5bd1e995u)*6.28318530718;let macroX=sin(tW*0.15+phaseMacro);let macroZ=cos(tW*0.18+phaseMacro*1.3);let gustX=sin(tW*0.70+phaseGust);let gustZ=sin(tW*0.85+phaseGust*0.9+1.2);let flutterX=sin(tW*3.0+phaseFlutter+input.position.z*7.0);let flutterZ=cos(tW*3.5+phaseFlutter*1.1+input.position.x*5.0);let windNoiseX=macroX*0.30+gustX*0.50+flutterX*0.20;let windNoiseZ=macroZ*0.30+gustZ*0.50+flutterZ*0.20;let windLen=max(length(vec2<f32>(windNoiseX,windNoiseZ)),0.001);let windNormX=windNoiseX/windLen;let windNormZ=windNoiseZ/windLen;let h=max(input.position.y,0.0);let hnMultiplier=2.8;let bladeH=min(h*hnMultiplier,1.0);let hn=clamp(bladeH,0.0,1.0);let hW=hn*hn;let hMid=hn*hn*(3.0-2.0*hn);let hTip=hn*hn*hn;let maxRad=material.advanced4.w;let offsetLower=0.12;let offsetMid=0.45;let offsetTip=1.00;let totalStrength=mix(offsetLower,offsetMid,hMid)*hW*0.6+offsetTip*hTip*0.4;let swayBase=maxRad*totalStrength;let swayRx=windNormX*swayBase;let swayRz=windNormZ*swayBase*0.6;$$TAN_FIX_WIND$$let bentLocal=rotateEulerXYZ(input.position,vec3<f32>(swayRx,0.0,swayRz));let bentN=rotateEulerXYZ(input.normal,vec3<f32>(swayRx,0.0,swayRz));worldPos4=inst.worldMatrix*vec4<f32>(bentLocal,1.0);let rotMatW=mat3x3<f32>(inst.worldMatrix[0].xyz,inst.worldMatrix[1].xyz,inst.worldMatrix[2].xyz);out.normal=normalize(rotMatW*bentN);}else if(animMode>0.5){let speedScale=max(0.01,material.advanced1.w);let t=camera.padding*speedScale;let posSeed=seedFromPosition(inst.worldMatrix[3].xyz);let baseSeed=posSeed^(realIndex*747796405u+2891336453u);let cellXZ=0.21;let cellY=0.95;let qp=vec3<f32>(floor(input.position.x/cellXZ+0.5)*cellXZ,floor(input.position.y/cellY+0.5)*cellY,floor(input.position.z/cellXZ+0.5)*cellXZ);let vSeed=baseSeed^bitcast<u32>(qp.x*173.0)^bitcast<u32>(qp.y*313.0)^bitcast<u32>(qp.z*563.0);let ph0=hash01_u32(vSeed)*6.28318530718;let ph1=hash01_u32(vSeed^0xa53c9a17u)*6.28318530718;let ph2=hash01_u32(vSeed^0x5bd1e995u)*6.28318530718;var amp=material.advanced4.w;if(amp<0.001){amp=0.02*speedScale;}amp=clamp(amp,0.003,0.08);let rel=input.position-qp;let edge=clamp(length(rel)/(cellXZ*0.68),0.15,1.0);let gust=vec3<f32>(sin(t*0.72+ph0),sin(t*0.58+ph1)*0.28,cos(t*0.69+ph2))*(amp*0.22);let ripple=vec3<f32>(sin(t*3.70+ph0+input.position.y*11.0+input.position.z*7.0),sin(t*4.25+ph1+input.position.x*13.0)*0.36,cos(t*3.45+ph2+input.position.x*5.0+input.position.y*9.0))*(amp*edge);let d=gust+ripple;let angles=vec3<f32>(sin(t*2.20+ph1)*amp*edge*1.2,cos(t*2.05+ph2)*amp*edge*1.6,sin(t*2.60+ph0)*amp*edge);$$TAN_FIX_EULER$$let flutterLocal=qp+rotateEulerXYZ(rel,angles)+d;worldPos4=inst.worldMatrix*vec4<f32>(flutterLocal,1.0);let rotMatW=mat3x3<f32>(inst.worldMatrix[0].xyz,inst.worldMatrix[1].xyz,inst.worldMatrix[2].xyz);out.normal=normalize(rotMatW*rotateEulerXYZ(input.normal,angles));}else{worldPos4=inst.worldMatrix*vec4<f32>(input.position,1.0);let rotMat=mat3x3<f32>(inst.worldMatrix[0].xyz,inst.worldMatrix[1].xyz,inst.worldMatrix[2].xyz);out.normal=normalize(rotMat*input.normal);}if(inst.alphaCutoff>0.001){let wpn=worldPos4.xyz;var eye=camera.position-wpn;let el=length(eye);if(el>1e-4){eye=eye/el;let zKey=(realIndex*747796405u)^bitcast<u32>(input.position.x*97.0)^bitcast<u32>(61.0*input.position.y)^bitcast<u32>(43.0*input.position.z);let zJ=hash01_u32(zKey);let pull=(zJ-0.5)*0.007;worldPos4=vec4<f32>(wpn+eye*pull,worldPos4.w);}}out.position=camera.viewProjection*worldPos4;out.worldPos=worldPos4.xyz;out.uv=input.uv;out.instanceIndex=realIndex;$$VS_MAIN_EXTRA$$return out;}fn fresnelSchlick(cosTheta:f32,F0:vec3<f32>)->vec3<f32>{return F0+(vec3<f32>(1.0)-F0)*pow(clamp(1.0-cosTheta,0.0,1.0),5.0);}fn distributionGGX(NdotH:f32,roughness:f32)->f32{let a=roughness*roughness;let a2=a*a;let NdotH2=NdotH*NdotH;return(a2)/max(3.14159265359*(NdotH2*(a2-1.0)+1.0)*(NdotH2*(a2-1.0)+1.0),0.0000001);}fn geometrySchlickGGX(NdotV:f32,roughness:f32)->f32{let r=(roughness+1.0);let k=(r*r)/8.0;return NdotV/max(NdotV*(1.0-k)+k,0.0000001);}fn geometrySmith(NdotV:f32,NdotL:f32,roughness:f32)->f32{return geometrySchlickGGX(NdotV,roughness)*geometrySchlickGGX(NdotL,roughness);}fn dielectricF0FromIOR(ior:f32)->f32{let i=clamp(ior,1.0,2.5);let f=(i-1.0)/(i+1.0);return f*f;}fn resolveTangentOrFallback(N:vec3<f32>,tangentOrZero:vec3<f32>)->vec3<f32>{if(length(tangentOrZero)>0.001){return normalize(tangentOrZero);}let up=select(vec3<f32>(0.0,1.0,0.0),vec3<f32>(1.0,0.0,0.0),abs(N.y)>0.99);return normalize(cross(up,N));}fn anisotropicRoughness(roughness:f32,anisotropy:f32,anisotropyRotation:f32,N:vec3<f32>,H:vec3<f32>,tangentOrZero:vec3<f32>)->f32{let a=clamp(anisotropy,-1.0,1.0);if(abs(a)<0.001){return clamp(roughness,0.02,1.0);}if(length(tangentOrZero)<=0.001){return clamp(roughness,0.02,1.0);}let T=resolveTangentOrFallback(N,tangentOrZero);let B=normalize(cross(N,T));let angle=clamp(anisotropyRotation,0.0,1.0)*6.28318530718;let Trot=normalize(T*cos(angle)+B*sin(angle));let hProj=H-N*dot(H,N);let hProjLen=length(hProj);if(hProjLen<0.0001){return clamp(roughness,0.02,1.0);}let hTan=hProj/hProjLen;let aligned=abs(dot(hTan,Trot));let rMin=max(0.02,roughness*(1.0-abs(a)*0.7));let rMax=min(1.0,roughness*(1.0+abs(a)*0.7));let blend=select(1.0-aligned,aligned,a>=0.0);return clamp(mix(rMin,rMax,blend),0.02,1.0);}fn envBRDFApprox(F0:vec3<f32>,roughness:f32,NdotV:f32)->vec3<f32>{let c0=vec4<f32>(-1.0,-0.0275,-0.572,0.022);let c1=vec4<f32>(1.0,0.0425,1.04,-0.04);let r=roughness*c0+c1;let a004=min(r.x*r.x,exp2(-9.28*NdotV))*r.x+r.y;let ab=vec2<f32>(-1.04,1.04)*a004+r.zw;return F0*ab.x+vec3<f32>(ab.y);}$$MATERIAL_PARALLAX_WGSL$$fn gridLineMask(worldXZ:vec2<f32>,stepMeters:f32,halfThicknessMeters:f32)->f32{let gx=abs(fract(worldXZ.x/stepMeters+0.5)-0.5)*stepMeters;let gz=abs(fract(worldXZ.y/stepMeters+0.5)-0.5)*stepMeters;let lineDist=min(gx,gz);return 1.0-smoothstep(halfThicknessMeters,halfThicknessMeters+0.012,lineDist);}fn terrainGridLineMask(uv:vec2<f32>,resolution:f32,stepCells:f32,halfThicknessCells:f32)->f32{let cell=uv*resolution;let gx=abs(fract(cell.x/stepCells+0.5)-0.5)*stepCells;let gz=abs(fract(cell.y/stepCells+0.5)-0.5)*stepCells;let lineDist=min(gx,gz);let aa=max(max(fwidth(cell.x),fwidth(cell.y))*1.35,0.006);return 1.0-smoothstep(halfThicknessCells,halfThicknessCells+aa,lineDist);}fn brushOverlayRingMask(worldXZ:vec2<f32>,overlay:vec4<f32>)->f32{let radius=overlay.z;let width=overlay.w;if(radius<=0.0||width<=0.0){return 0.0;}let distanceToCenter=length(worldXZ-overlay.xy);let ringDistance=abs(distanceToCenter-radius);let halfWidth=max(width*0.5,0.001);let aa=max(fwidth(distanceToCenter)*1.45,0.025);return 1.0-smoothstep(halfWidth,halfWidth+aa,ringDistance);}fn cookTorranceBRDF(N:vec3<f32>,V:vec3<f32>,L:vec3<f32>,albedo:vec3<f32>,metallic:f32,roughness:f32,F0:vec3<f32>,transmission:f32)->vec3<f32>{let wrap=0.0;let H=normalize(V+L);let NdotV=max(dot(N,V),0.0000001);let NdotL=max(dot(N,L),0.0000001);let wrappedNdotL=max((dot(N,L)+wrap)/(1.0+wrap),0.0);let NdotH=max(dot(N,H),0.0);let VdotH=max(dot(V,H),0.0);let F=fresnelSchlick(VdotH,F0);let D=distributionGGX(NdotH,roughness);let G=geometrySmith(NdotV,NdotL,roughness);let numerator=D*G*F;let denominator=4.0*NdotV*NdotL;let specular=min(numerator/max(denominator,0.0000001),vec3<f32>(4.0));let kD=(vec3<f32>(1.0)-F)*(1.0-metallic)*(1.0-transmission);let diffuseTerm=(kD*albedo/3.14159265359)*wrappedNdotL;let specularTerm=specular*NdotL*GLOBAL_SPECULAR_DAMP;return diffuseTerm+specularTerm;}fn clearcoatSpecular(N:vec3<f32>,V:vec3<f32>,L:vec3<f32>,clearcoatRoughness:f32)->vec3<f32>{let H=normalize(V+L);let NdotV=max(dot(N,V),0.0000001);let NdotL=max(dot(N,L),0.0000001);let NdotH=max(dot(N,H),0.0);let VdotH=max(dot(V,H),0.0);let F=fresnelSchlick(VdotH,vec3<f32>(0.04));let D=distributionGGX(NdotH,clearcoatRoughness);let G=geometrySmith(NdotV,NdotL,clearcoatRoughness);let spec=(D*G*F)/max(4.0*NdotV*NdotL,0.0000001);return spec*NdotL*GLOBAL_SPECULAR_DAMP;}$$VELOCITY_FRAGMENT_STRUCT$$@fragment fn fs_main(in:VertexOutput,@builtin(front_facing)front_facing:bool)->$$FRAGMENT_RETURN_TYPE$${let inst=entityBuffer[in.instanceIndex];let N_geom=select(-normalize(in.normal),normalize(in.normal),front_facing);var N=N_geom;let distToCamera=max(length(camera.position-in.worldPos),0.0001);let V=(camera.position-in.worldPos)/distToCamera;var baseUv=materialBaseUv(in.uv);var surfaceTangent=$$PARALLAX_TANGENT$$;var surfaceTangentW=$$PARALLAX_TANGENT_W$$;if(inst.surfaceGridEnable>1.5){let nAbs=abs(N_geom);var projectedUv=in.worldPos.xz;let worldUvScale=0.55;if(inst.surfaceGridEnable>2.5){let downXZRaw=vec2<f32>(N_geom.x,N_geom.z);let downXZLen=max(length(downXZRaw),1e-4);let downXZ=downXZRaw/downXZLen;let ridgeXZ=vec2<f32>(-downXZ.y,downXZ.x);projectedUv=vec2<f32>(dot(in.worldPos.xz,ridgeXZ),dot(in.worldPos.xz,downXZ));surfaceTangent=normalize(vec3<f32>(ridgeXZ.x,0.0,ridgeXZ.y));surfaceTangentW=1.0;}else if(nAbs.y>=nAbs.x&&nAbs.y>=nAbs.z){projectedUv=in.worldPos.xz;surfaceTangent=vec3<f32>(1.0,0.0,0.0);surfaceTangentW=1.0;}else if(nAbs.x>=nAbs.z){projectedUv=vec2<f32>(in.worldPos.z,in.worldPos.y);surfaceTangent=vec3<f32>(0.0,0.0,1.0);surfaceTangentW=1.0;}else{projectedUv=vec2<f32>(in.worldPos.x,in.worldPos.y);surfaceTangent=vec3<f32>(1.0,0.0,0.0);surfaceTangentW=1.0;}baseUv=materialBaseUv(projectedUv*worldUvScale);}var uv=baseUv;if(distToCamera<60.0){uv=parallaxOcclusionMapping(baseUv,V,N,material.params.w,surfaceTangent,surfaceTangentW,material.advanced3.z,material.advanced4.x,material.advanced4.y,material.advanced4.z);}let albedoTexColor=textureSample(albedoTexture,materialSampler,uv);var baseColor=albedoTexColor.rgb*material.albedo.rgb*inst.color.rgb$$VERTEX_COLOR_MUL$$;baseColor*=materialMacroVariation(in.worldPos,N_geom,material.advanced3.z);baseColor=max(baseColor,vec3<f32>(0.018,0.011,0.007));var alpha=albedoTexColor.a*material.albedo.a$$VERTEX_COLOR_ALPHA$$;let meshAlphaCutoff=inst.alphaCutoff;let textureMaskCutoff=select(0.0,0.5,meshAlphaCutoff<=0.001&&material.albedo.a>=0.999&&albedoTexColor.a<0.999);let effectiveAlphaCutoff=max(meshAlphaCutoff,textureMaskCutoff);let alphaMaskedOpaque=effectiveAlphaCutoff>0.0&&material.albedo.a>=0.999;if(effectiveAlphaCutoff>0.0){if(alpha<effectiveAlphaCutoff){discard;}alpha=1.0;}let mrSample=textureSample(metallicRoughnessTexture,materialSampler,uv);let finalMetallic=material.params.x*mrSample.b;let finalRoughness=clamp(material.params.y*mrSample.g,0.03,1.0);let clearcoat=clamp(material.advanced0.x,0.0,1.0);let clearcoatRoughness=clamp(material.advanced0.y,0.03,1.0);let transmission=clamp(material.advanced0.z,0.0,1.0)*(1.0-finalMetallic);let ior=clamp(material.advanced0.w,1.0,2.5);let anisotropy=clamp(material.advanced1.x,-1.0,1.0);let anisotropyRotation=clamp(material.advanced1.y,0.0,1.0);let dielectricF0=dielectricF0FromIOR(ior);let baseF0=mix(vec3<f32>(dielectricF0),baseColor,finalMetallic);let normalMapSample=textureSample(normalTexture,materialSampler,uv);let tangentOrZero=surfaceTangent;if(length(tangentOrZero)>0.001&&distToCamera<180.0){var normalMapTS=normalMapSample.rgb*2.0-vec3<f32>(1.0);let normalStrength=material.advanced3.x;normalMapTS=normalize(vec3<f32>(normalMapTS.xy*normalStrength,normalMapTS.z));let T=normalize(tangentOrZero);let B=cross(N,T)*select(-1.0,1.0,surfaceTangentW>=0.0);let TBN=mat3x3<f32>(T,B,N);N=normalize(TBN*normalMapTS);}let concreteLike=select(0.0,1.0,abs(baseColor.r-baseColor.g)<0.06&&abs(baseColor.g-baseColor.b)<0.06)*select(0.0,1.0,finalMetallic<0.2&&finalRoughness>0.4);let upwardLitGate=smoothstep(0.0,0.35,N.y);let litFlatten=0.58*concreteLike*upwardLitGate;let N_lit=normalize(mix(N,vec3<f32>(0.0,1.0,0.0),litFlatten));let shadowReceiveGate=1.0;let roofUndersideGeomMul=mix(0.40,1.0,smoothstep(-0.95,-0.45,N_geom.y));let backfaceShadowMul=select(0.65,1.0,front_facing);let shadowRecvForMap=inst.receiveShadows*shadowReceiveGate*roofUndersideGeomMul*backfaceShadowMul;var directLighting=vec3<f32>(0.0);let lightCount=getLightCount();for(var i=0;i<MAX_LIGHTS;i++){if(i>=lightCount){break;}let light=getLightAt(i);let lightData=getLightDirectionAndDistance(light,in.worldPos);let L=lightData.xyz;let lightDistance=lightData.w;let geomSunGate=select(1.0,directionalSunShadowReceiveGate(L,N_geom),light.direction.w<0.5);let recvShadows=shadowRecvForMap*geomSunGate;let applyShadow=shouldApplyShadowForLight(i,light.direction.w);let pomSelfShadow=select(1.0,parallaxSelfShadow(uv,L,N_geom,material.params.w,tangentOrZero,surfaceTangentW,material.advanced3.z),distToCamera<60.0);let radiance=getLightRadiance(i,light,in.worldPos,N_geom,recvShadows,L,lightDistance,applyShadow,inst.shadowReceiverBiasMul)*2.0*pomSelfShadow;let H=normalize(V+L);let anisoRoughness=anisotropicRoughness(finalRoughness,anisotropy,anisotropyRotation,N_lit,H,tangentOrZero);directLighting+=cookTorranceBRDF(N_lit,V,L,baseColor,finalMetallic,anisoRoughness,baseF0,transmission)*radiance;directLighting+=clearcoatSpecular(N_lit,V,L,clearcoatRoughness)*radiance*(clearcoat*0.25);}let R=reflect(-V,N_lit);let refractDir=refract(-V,N_lit,1.0/ior);let NdotV=max(dot(N_lit,V),0.0);let F_env=fresnelSchlick(NdotV,baseF0);let reflectionStrength=(1.0-finalRoughness)*max(finalMetallic+dielectricF0*(1.0-finalMetallic),0.04);let planarWeight=clamp((1.0-finalRoughness)*(0.25+finalMetallic*0.75+clearcoat*0.45),0.0,1.0);let isFloorLike=abs(in.worldPos.y)<=0.12&&N_geom.y>=0.7;let worldPos4=vec4<f32>(in.worldPos,1.0);let reflectionClip=reflectionCamera.matrix*worldPos4;let reflectionW=max(abs(reflectionClip.w),0.000001);let reflectionNDC=reflectionClip.xyz/reflectionW;let reflectionInFrustum=abs(reflectionNDC.x)<=1.0&&abs(reflectionNDC.y)<=1.0&&reflectionNDC.z>=-1.0&&reflectionNDC.z<=1.0;var reflectionUV=clamp(vec2<f32>(reflectionNDC.x*0.5+0.5,-reflectionNDC.y*0.5+0.5),vec2<f32>(0.0),vec2<f32>(1.0));let reflectionColor=textureSample(reflectionTexture,reflectionSampler,reflectionUV);let specularIBL=getSpecularIBL(R,finalRoughness,NdotV,baseF0);let planarEnable=reflectionInFrustum&&isFloorLike&&planarWeight>0.2;let reflectionBase=select(specularIBL,reflectionColor.rgb,planarEnable);let iblIntensity=getIblIntensity();let topDownAtt=getTopDownSpecularAttenuation(V);var reflection=reflectionBase*reflectionStrength*iblIntensity*topDownAtt*GLOBAL_SPECULAR_DAMP;let clearcoatSpecIBL=getSpecularIBL(R,clearcoatRoughness,NdotV,vec3<f32>(0.04));var clearcoatReflection=clearcoatSpecIBL*(clearcoat*0.35)*iblIntensity*topDownAtt*GLOBAL_SPECULAR_DAMP;let ao=pow(max(textureSample(aoTexture,materialSampler,uv).r,0.001),clamp(material.advanced3.y,0.0,4.0));let ambientStrength=getAmbientStrength();let ambientColor=getAmbientColor();let kD=(vec3<f32>(1.0)-F_env)*(1.0-finalMetallic)*(1.0-transmission);let ambientNormal=N_geom;let diffuseIBL=getDiffuseIndirect(ambientNormal,in.worldPos);var diffuseAmbient=kD*baseColor*(diffuseIBL*(0.5*iblIntensity)+ambientStrength*0.25+ambientColor*uShadowSettings.ambientIntensity)*ao;let hemi=mix(1.22,1.04,clamp(ambientNormal.y*0.5+0.5,0.0,1.0));let undersideLift=smoothstep(0.0,0.65,-ambientNormal.y);diffuseAmbient*=hemi*mix(1.0,1.0,undersideLift);let transmissionEnv=getDiffuseIndirect(refractDir,in.worldPos);let transmissionTerm=transmissionEnv*baseColor*transmission*(1.0-finalRoughness*0.75)*ao*iblIntensity;let metalAmbientLift=getDiffuseIndirect(ambientNormal,in.worldPos)*baseColor*finalMetallic*(0.12+ambientStrength*0.18)*ao*iblIntensity;let ambient=diffuseAmbient+reflection+clearcoatReflection+transmissionTerm+metalAmbientLift;let emissiveColor=textureSample(emissiveTexture,materialSampler,uv).rgb*material.params.z;var finalColor=directLighting+ambient+emissiveColor;let foliageLightFloor=select(0.0,0.18,alphaMaskedOpaque);finalColor=max(finalColor,baseColor*foliageLightFloor);let terrainCellStepMeters=max(0.001,material.advanced4.z);let terrainChunkStepCells=max(1.0,round(material.advanced4.x));let terrainRegionStepCells=max(terrainChunkStepCells,round(material.advanced4.y));let terrainChunkStepMeters=terrainCellStepMeters*terrainChunkStepCells;let terrainRegionStepMeters=terrainCellStepMeters*terrainRegionStepCells;let terrainRegionMask=gridLineMask(in.worldPos.xz,terrainRegionStepMeters,max(0.06,terrainCellStepMeters*0.06));let terrainChunkRawMask=gridLineMask(in.worldPos.xz,terrainChunkStepMeters,max(0.045,terrainCellStepMeters*0.045));let terrainCellRawMask=gridLineMask(in.worldPos.xz,terrainCellStepMeters,max(0.022,terrainCellStepMeters*0.022));if(inst.surfaceGridEnable>0.5&&inst.surfaceGridEnable<1.5){let major5m=gridLineMask(in.worldPos.xz,5.0,0.050);let minor1mRaw=gridLineMask(in.worldPos.xz,1.0,0.016);let minor1m=clamp(minor1mRaw-major5m*0.9,0.0,1.0);let minorColor=vec3<f32>(0.40,0.44,0.50);let majorColor=vec3<f32>(0.78,0.81,0.86);finalColor=mix(finalColor,minorColor,minor1m*0.55);finalColor=mix(finalColor,majorColor,major5m*0.88);}else if(inst.surfaceGridEnable<-0.5){let gridModeEncoded=u32(max(0.0,round(abs(inst.surfaceGridEnable))));let gridMask=select(0u,gridModeEncoded-1u,gridModeEncoded>=1u);let showCell=(gridMask&1u)!=0u;let showChunk=(gridMask&2u)!=0u;let showRegion=(gridMask&4u)!=0u;let showCollision=(gridMask&8u)!=0u;let region=terrainRegionMask;let chunk=clamp(terrainChunkRawMask-region*0.95,0.0,1.0);let cell=clamp(terrainCellRawMask-max(chunk,region)*0.95,0.0,1.0);let collision=clamp(terrainCellRawMask-max(chunk,region)*0.95,0.0,1.0);let cellColor=vec3<f32>(0.92,0.96,1.0);let collisionColor=vec3<f32>(0.05,0.82,0.95);let chunkColor=vec3<f32>(1.0,0.55,0.10);let regionColor=vec3<f32>(1.0,0.08,0.08);finalColor=mix(finalColor,cellColor,select(0.0,cell*0.24,showCell));finalColor=mix(finalColor,collisionColor,select(0.0,collision*0.32,showCollision));finalColor=mix(finalColor,chunkColor,select(0.0,chunk*0.76,showChunk));finalColor=mix(finalColor,regionColor,select(0.0,region*0.94,showRegion));}let brushOverlayMask=brushOverlayRingMask(in.worldPos.xz,material.brushOverlay);finalColor=mix(finalColor,vec3<f32>(1.0,0.84,0.06),brushOverlayMask*0.92);let debugView=u32(material.advanced3.w);if(debugView==1u){finalColor=baseColor;}if(debugView==2u){finalColor=normalMapSample.rgb;}if(debugView==3u){finalColor=vec3<f32>(finalRoughness);}if(debugView==4u){finalColor=vec3<f32>(finalMetallic);}if(debugView==5u){finalColor=vec3<f32>(ao);}if(debugView==6u){finalColor=vec3<f32>(textureSample(heightTexture,materialSampler,uv).r);}if(debugView==7u){finalColor=vec3<f32>(fract(uv),0.0);}if(debugView==8u){finalColor=abs(normalize(N))*0.85+vec3<f32>(0.075);}finalColor=applyFog(finalColor,in.worldPos,camera.position);let outColor=vec4<f32>(finalColor,alpha);$$VELOCITY_CALC$$$$FRAGMENT_RETURN$$}\n".replace(/\$\$LIGHTING_COMMON\$\$/g,o),x=65,b="struct Camera{viewProjection:mat4x4<f32>,position:vec3<f32>,padding:f32,prevViewProjection:mat4x4<f32>,};@group(0)@binding(0)var<uniform>camera:Camera;$$LIGHTING_COMMON$$@group(0)@binding(4)var envMap:texture_2d<f32>;fn sampleEquirectangular(dir:vec3<f32>)->vec3<f32>{let n=normalize(dir);let lon=atan2(n.z,n.x);let lat=asin(clamp(n.y,-1.0,1.0));let u=fract(lon/6.28318530718+0.5);let v=0.5-(lat/3.14159265359);return textureSample(envMap,iblSampler,vec2<f32>(u,clamp(v,0.0,1.0))).rgb;}fn hash01_u32(x:u32)->f32{let n=f32(x)*0.1031;return fract(sin(n*12.9898+78.233)*43758.5453);}fn seedFromPosition(p:vec3<f32>)->u32{let h=sin(dot(p,vec3<f32>(12.9898,78.233,37.719)));return u32(abs(h)*4294967295.0);}fn rotateAroundAxis(v:vec3<f32>,axisRaw:vec3<f32>,angle:f32)->vec3<f32>{let axis=normalize(axisRaw+vec3<f32>(0.0001,0.0,0.0));let s=sin(angle);let c=cos(angle);return v*c+cross(axis,v)*s+axis*dot(axis,v)*(1.0-c);}fn rotateEulerXYZ(v:vec3<f32>,angles:vec3<f32>)->vec3<f32>{let sx=sin(angles.x);let cx=cos(angles.x);let sy=sin(angles.y);let cy=cos(angles.y);let sz=sin(angles.z);let cz=cos(angles.z);let rx=vec3<f32>(v.x,v.y*cx-v.z*sx,v.y*sx+v.z*cx);let ry=vec3<f32>(rx.x*cy+rx.z*sy,rx.y,-rx.x*sy+rx.z*cy);return vec3<f32>(ry.x*cz-ry.y*sz,ry.x*sz+ry.y*cz,ry.z);}struct MaterialUniforms{albedo:vec4<f32>,params:vec4<f32>,advanced0:vec4<f32>,advanced1:vec4<f32>,advanced2:vec4<f32>,advanced3:vec4<f32>,advanced4:vec4<f32>,};@group(2)@binding(0)var<uniform>material:MaterialUniforms;@group(2)@binding(1)var albedoTexture:texture_2d<f32>;@group(2)@binding(2)var normalTexture:texture_2d<f32>;@group(2)@binding(3)var metallicRoughnessTexture:texture_2d<f32>;@group(2)@binding(4)var aoTexture:texture_2d<f32>;@group(2)@binding(5)var emissiveTexture:texture_2d<f32>;@group(2)@binding(6)var heightTexture:texture_2d<f32>;@group(2)@binding(7)var materialSampler:sampler;@group(2)@binding(8)var reflectionTexture:texture_2d<f32>;@group(2)@binding(9)var reflectionSampler:sampler;struct ReflectionCameraData{matrix:mat4x4<f32>}@group(2)@binding(10)var<uniform>reflectionCamera:ReflectionCameraData;struct EntityInstance{worldMatrix:mat4x4<f32>,color:vec4<f32>,receiveShadows:f32,alphaCutoff:f32,shadowReceiverBiasMul:f32,meshId:u32,materialId:u32,};struct MeshletExpandedVertex{pos:vec4<f32>,nrm:vec4<f32>,uv:vec2<f32>,entity:u32,_pad:u32,}@group(3)@binding(0)var<storage,read>entityBuffer:array<EntityInstance>;@group(3)@binding(1)var<storage,read>meshletExpanded:array<MeshletExpandedVertex>;struct VertexOutput{@builtin(position)position:vec4<f32>,@location(0)worldPos:vec3<f32>,@location(1)normal:vec3<f32>,@location(2)uv:vec2<f32>$$VERTEX_OUTPUT_EXTRA$$,@location(5)@interpolate(flat)instanceIndex:u32};const GLOBAL_SPECULAR_DAMP:f32=0.28;@vertex fn vs_main(@builtin(vertex_index)vid:u32)->VertexOutput{var out:VertexOutput;let mv=meshletExpanded[vid];let realIndex=mv.entity;let inst=entityBuffer[realIndex];let localPos=mv.pos.xyz;let localNrm=mv.nrm.xyz;let inUv=mv.uv;var worldPos4:vec4<f32>;let animMode=material.advanced1.z;if(animMode>1.5){let speedScale=max(0.01,material.advanced1.w);let tW=camera.padding*speedScale;let posSeedW=seedFromPosition(inst.worldMatrix[3].xyz);let baseSeedW=posSeedW^(realIndex*747796405u+2891336453u);let phaseXW=hash01_u32(baseSeedW+0x94d049bbu)*6.28318530718;let phaseZW=hash01_u32(baseSeedW+0x7f4a7c15u)*6.28318530718;let nxW=sin(tW*1.18+phaseXW);let nzW=sin(tW*1.47+phaseZW);let h=max(localPos.y,0.0);let hnMultiplier=2.8;let hn=clamp(h*hnMultiplier,0.0,1.0);let hW=hn*hn;let maxRad=material.advanced4.w;let swayRx=nxW*maxRad*hW;let swayRz=nzW*maxRad*hW;let bentLocal=rotateEulerXYZ(localPos,vec3<f32>(swayRx,0.0,swayRz));let bentN=rotateEulerXYZ(localNrm,vec3<f32>(swayRx,0.0,swayRz));worldPos4=inst.worldMatrix*vec4<f32>(bentLocal,1.0);let rotMatW=mat3x3<f32>(inst.worldMatrix[0].xyz,inst.worldMatrix[1].xyz,inst.worldMatrix[2].xyz);out.normal=normalize(rotMatW*bentN);}else if(animMode>0.5){let speedScale=max(0.01,material.advanced1.w);let t=camera.padding*speedScale;let posSeed=seedFromPosition(inst.worldMatrix[3].xyz);let baseSeed=posSeed^(realIndex*747796405u+2891336453u);let cell=0.13;let qp=vec3<f32>(floor(localPos.x/cell+0.5)*cell,floor(localPos.y/cell+0.5)*cell,floor(localPos.z/cell+0.5)*cell);let vSeed=baseSeed^bitcast<u32>(qp.x*173.0)^bitcast<u32>(qp.y*313.0)^bitcast<u32>(qp.z*563.0);let ph0=hash01_u32(vSeed)*6.28318530718;let ph1=hash01_u32(vSeed^0xa53c9a17u)*6.28318530718;let ph2=hash01_u32(vSeed^0x5bd1e995u)*6.28318530718;var amp=material.advanced4.w;if(amp<0.001){amp=0.02*speedScale;}amp=clamp(amp,0.004,0.28);let d=vec3<f32>(sin(t*1.21+ph0)*amp,sin(t*0.88+ph1)*amp*0.42,cos(t*1.09+ph2)*amp);let flutterLocal=localPos+d;worldPos4=inst.worldMatrix*vec4<f32>(flutterLocal,1.0);let rotMatW=mat3x3<f32>(inst.worldMatrix[0].xyz,inst.worldMatrix[1].xyz,inst.worldMatrix[2].xyz);out.normal=normalize(rotMatW*localNrm);}else{worldPos4=inst.worldMatrix*vec4<f32>(localPos,1.0);let rotMat=mat3x3<f32>(inst.worldMatrix[0].xyz,inst.worldMatrix[1].xyz,inst.worldMatrix[2].xyz);out.normal=normalize(rotMat*localNrm);}out.position=camera.viewProjection*worldPos4;out.worldPos=worldPos4.xyz;out.uv=inUv;out.instanceIndex=realIndex;$$VS_MAIN_EXTRA$$return out;}fn fresnelSchlick(cosTheta:f32,F0:vec3<f32>)->vec3<f32>{return F0+(vec3<f32>(1.0)-F0)*pow(clamp(1.0-cosTheta,0.0,1.0),5.0);}fn distributionGGX(NdotH:f32,roughness:f32)->f32{let a=roughness*roughness;let a2=a*a;let NdotH2=NdotH*NdotH;return(a2)/max(3.14159265359*(NdotH2*(a2-1.0)+1.0)*(NdotH2*(a2-1.0)+1.0),0.0000001);}fn geometrySchlickGGX(NdotV:f32,roughness:f32)->f32{let r=(roughness+1.0);let k=(r*r)/8.0;return NdotV/max(NdotV*(1.0-k)+k,0.0000001);}fn geometrySmith(NdotV:f32,NdotL:f32,roughness:f32)->f32{return geometrySchlickGGX(NdotV,roughness)*geometrySchlickGGX(NdotL,roughness);}fn dielectricF0FromIOR(ior:f32)->f32{let i=clamp(ior,1.0,2.5);let f=(i-1.0)/(i+1.0);return f*f;}fn resolveTangentOrFallback(N:vec3<f32>,tangentOrZero:vec3<f32>)->vec3<f32>{if(length(tangentOrZero)>0.001){return normalize(tangentOrZero);}let up=select(vec3<f32>(0.0,1.0,0.0),vec3<f32>(1.0,0.0,0.0),abs(N.y)>0.99);return normalize(cross(up,N));}fn anisotropicRoughness(roughness:f32,anisotropy:f32,anisotropyRotation:f32,N:vec3<f32>,H:vec3<f32>,tangentOrZero:vec3<f32>)->f32{let a=clamp(anisotropy,-1.0,1.0);if(abs(a)<0.001){return clamp(roughness,0.02,1.0);}if(length(tangentOrZero)<=0.001){return clamp(roughness,0.02,1.0);}let T=resolveTangentOrFallback(N,tangentOrZero);let B=normalize(cross(N,T));let angle=clamp(anisotropyRotation,0.0,1.0)*6.28318530718;let Trot=normalize(T*cos(angle)+B*sin(angle));let hProj=H-N*dot(H,N);let hProjLen=length(hProj);if(hProjLen<0.0001){return clamp(roughness,0.02,1.0);}let hTan=hProj/hProjLen;let aligned=abs(dot(hTan,Trot));let rMin=max(0.02,roughness*(1.0-abs(a)*0.7));let rMax=min(1.0,roughness*(1.0+abs(a)*0.7));let blend=select(1.0-aligned,aligned,a>=0.0);return clamp(mix(rMin,rMax,blend),0.02,1.0);}fn envBRDFApprox(F0:vec3<f32>,roughness:f32,NdotV:f32)->vec3<f32>{let c0=vec4<f32>(-1.0,-0.0275,-0.572,0.022);let c1=vec4<f32>(1.0,0.0425,1.04,-0.04);let r=roughness*c0+c1;let a004=min(r.x*r.x,exp2(-9.28*NdotV))*r.x+r.y;let ab=vec2<f32>(-1.04,1.04)*a004+r.zw;return F0*ab.x+vec3<f32>(ab.y);}$$MATERIAL_PARALLAX_WGSL$$fn gridLineMask(worldXZ:vec2<f32>,stepMeters:f32,halfThicknessMeters:f32)->f32{let gx=abs(fract(worldXZ.x/stepMeters+0.5)-0.5)*stepMeters;let gz=abs(fract(worldXZ.y/stepMeters+0.5)-0.5)*stepMeters;let lineDist=min(gx,gz);return 1.0-smoothstep(halfThicknessMeters,halfThicknessMeters+0.012,lineDist);}fn cookTorranceBRDF(N:vec3<f32>,V:vec3<f32>,L:vec3<f32>,albedo:vec3<f32>,metallic:f32,roughness:f32,F0:vec3<f32>,transmission:f32)->vec3<f32>{let wrap=0.0;let H=normalize(V+L);let NdotV=max(dot(N,V),0.0000001);let NdotL=max(dot(N,L),0.0000001);let wrappedNdotL=max((dot(N,L)+wrap)/(1.0+wrap),0.0);let NdotH=max(dot(N,H),0.0);let VdotH=max(dot(V,H),0.0);let F=fresnelSchlick(VdotH,F0);let D=distributionGGX(NdotH,roughness);let G=geometrySmith(NdotV,NdotL,roughness);let numerator=D*G*F;let denominator=4.0*NdotV*NdotL;let specular=min(numerator/max(denominator,0.0000001),vec3<f32>(4.0));let kD=(vec3<f32>(1.0)-F)*(1.0-metallic)*(1.0-transmission);let diffuseTerm=(kD*albedo/3.14159265359)*wrappedNdotL;let specularTerm=specular*NdotL*GLOBAL_SPECULAR_DAMP;return diffuseTerm+specularTerm;}fn clearcoatSpecular(N:vec3<f32>,V:vec3<f32>,L:vec3<f32>,clearcoatRoughness:f32)->vec3<f32>{let H=normalize(V+L);let NdotV=max(dot(N,V),0.0000001);let NdotL=max(dot(N,L),0.0000001);let NdotH=max(dot(N,H),0.0);let VdotH=max(dot(V,H),0.0);let F=fresnelSchlick(VdotH,vec3<f32>(0.04));let D=distributionGGX(NdotH,clearcoatRoughness);let G=geometrySmith(NdotV,NdotL,clearcoatRoughness);let spec=(D*G*F)/max(4.0*NdotV*NdotL,0.0000001);return spec*NdotL*GLOBAL_SPECULAR_DAMP;}$$VELOCITY_FRAGMENT_STRUCT$$@fragment fn fs_main(in:VertexOutput,@builtin(front_facing)front_facing:bool)->$$FRAGMENT_RETURN_TYPE$${let inst=entityBuffer[in.instanceIndex];let N_geom=select(-normalize(in.normal),normalize(in.normal),front_facing);var N=N_geom;let distToCamera=length(camera.position-in.worldPos);let V=(camera.position-in.worldPos)/distToCamera;let baseUv=materialBaseUv(in.uv);var uv=baseUv;if(distToCamera<60.0){uv=parallaxOcclusionMapping(baseUv,V,N,material.params.w,$$PARALLAX_TANGENT$$,$$PARALLAX_TANGENT_W$$,material.advanced3.z,material.advanced4.x,material.advanced4.y,material.advanced4.z);}let albedoTexColor=textureSample(albedoTexture,materialSampler,uv);var baseColor=albedoTexColor.rgb*material.albedo.rgb*inst.color.rgb$$VERTEX_COLOR_MUL$$;baseColor*=materialMacroVariation(in.worldPos,N_geom,material.advanced3.z);baseColor=max(baseColor,vec3<f32>(0.018,0.011,0.007));var alpha=albedoTexColor.a*material.albedo.a$$VERTEX_COLOR_ALPHA$$;let meshAlphaCutoff=inst.alphaCutoff;let textureMaskCutoff=select(0.0,0.5,meshAlphaCutoff<=0.001&&material.albedo.a>=0.999&&albedoTexColor.a<0.999);let effectiveAlphaCutoff=max(meshAlphaCutoff,textureMaskCutoff);let alphaMaskedOpaque=effectiveAlphaCutoff>0.0&&material.albedo.a>=0.999;if(effectiveAlphaCutoff>0.0){if(alpha<effectiveAlphaCutoff){discard;}alpha=1.0;}let mrSample=textureSample(metallicRoughnessTexture,materialSampler,uv);let finalMetallic=material.params.x*mrSample.b;let finalRoughness=clamp(material.params.y*mrSample.g,0.03,1.0);let clearcoat=clamp(material.advanced0.x,0.0,1.0);let clearcoatRoughness=clamp(material.advanced0.y,0.03,1.0);let transmission=clamp(material.advanced0.z,0.0,1.0)*(1.0-finalMetallic);let ior=clamp(material.advanced0.w,1.0,2.5);let anisotropy=clamp(material.advanced1.x,-1.0,1.0);let anisotropyRotation=clamp(material.advanced1.y,0.0,1.0);let dielectricF0=dielectricF0FromIOR(ior);let baseF0=mix(vec3<f32>(dielectricF0),baseColor,finalMetallic);let normalMapSample=textureSample(normalTexture,materialSampler,uv);let tangentOrZero=$$PARALLAX_TANGENT$$;if(length(tangentOrZero)>0.001&&distToCamera<180.0){var normalMapTS=normalMapSample.rgb*2.0-vec3<f32>(1.0);let normalStrength=material.advanced3.x;normalMapTS=normalize(vec3<f32>(normalMapTS.xy*normalStrength,normalMapTS.z));let T=normalize(tangentOrZero);let B=cross(N,T)*select(-1.0,1.0,$$PARALLAX_TANGENT_W$$>=0.0);let TBN=mat3x3<f32>(T,B,N);N=normalize(TBN*normalMapTS);}let concreteLike=select(0.0,1.0,abs(baseColor.r-baseColor.g)<0.06&&abs(baseColor.g-baseColor.b)<0.06)*select(0.0,1.0,finalMetallic<0.2&&finalRoughness>0.4);let upwardLitGate=smoothstep(0.0,0.35,N.y);let litFlatten=0.58*concreteLike*upwardLitGate;let N_lit=normalize(mix(N,vec3<f32>(0.0,1.0,0.0),litFlatten));let shadowReceiveGate=1.0;let roofUndersideGeomMul=mix(0.40,1.0,smoothstep(-0.95,-0.45,N_geom.y));let backfaceShadowMul=select(0.65,1.0,front_facing);let shadowRecvForMap=inst.receiveShadows*shadowReceiveGate*roofUndersideGeomMul*backfaceShadowMul;var directLighting=vec3<f32>(0.0);let lightCount=getLightCount();for(var i=0;i<MAX_LIGHTS;i++){if(i>=lightCount){break;}let light=getLightAt(i);let lightData=getLightDirectionAndDistance(light,in.worldPos);let L=lightData.xyz;let lightDistance=lightData.w;let geomSunGate=select(1.0,directionalSunShadowReceiveGate(L,N_geom),light.direction.w<0.5);let recvShadows=shadowRecvForMap*geomSunGate;let applyShadow=shouldApplyShadowForLight(i,light.direction.w);let pomSelfShadow=select(1.0,parallaxSelfShadow(uv,L,N_geom,material.params.w,tangentOrZero,$$PARALLAX_TANGENT_W$$,material.advanced3.z),distToCamera<60.0);var radiance=getLightRadiance(i,light,in.worldPos,N_geom,recvShadows,L,lightDistance,applyShadow,inst.shadowReceiverBiasMul)*2.0*pomSelfShadow;let H=normalize(V+L);let anisoRoughness=anisotropicRoughness(finalRoughness,anisotropy,anisotropyRotation,N_lit,H,tangentOrZero);directLighting+=cookTorranceBRDF(N_lit,V,L,baseColor,finalMetallic,anisoRoughness,baseF0,transmission)*radiance;directLighting+=clearcoatSpecular(N_lit,V,L,clearcoatRoughness)*radiance*(clearcoat*0.25);}let R=reflect(-V,N_lit);let refractDir=refract(-V,N_lit,1.0/ior);let NdotV=max(dot(N_lit,V),0.0);let F_env=fresnelSchlick(NdotV,baseF0);let reflectionStrength=(1.0-finalRoughness)*max(finalMetallic+dielectricF0*(1.0-finalMetallic),0.04);let planarWeight=clamp((1.0-finalRoughness)*(0.25+finalMetallic*0.75+clearcoat*0.45),0.0,1.0);let isFloorLike=abs(in.worldPos.y)<=0.12&&N_geom.y>=0.7;let worldPos4=vec4<f32>(in.worldPos,1.0);let reflectionClip=reflectionCamera.matrix*worldPos4;let reflectionW=max(abs(reflectionClip.w),0.000001);let reflectionNDC=reflectionClip.xyz/reflectionW;let reflectionInFrustum=abs(reflectionNDC.x)<=1.0&&abs(reflectionNDC.y)<=1.0&&reflectionNDC.z>=-1.0&&reflectionNDC.z<=1.0;var reflectionUV=clamp(vec2<f32>(reflectionNDC.x*0.5+0.5,-reflectionNDC.y*0.5+0.5),vec2<f32>(0.0),vec2<f32>(1.0));let reflectionColor=textureSample(reflectionTexture,reflectionSampler,reflectionUV);let specularIBL=getSpecularIBL(R,finalRoughness,NdotV,baseF0);let planarEnable=reflectionInFrustum&&isFloorLike&&planarWeight>0.2;let reflectionBase=select(specularIBL,reflectionColor.rgb,planarEnable);let iblIntensity=getIblIntensity();let topDownAtt=getTopDownSpecularAttenuation(V);var reflection=reflectionBase*reflectionStrength*iblIntensity*topDownAtt*GLOBAL_SPECULAR_DAMP;let clearcoatSpecIBL=getSpecularIBL(R,clearcoatRoughness,NdotV,vec3<f32>(0.04));var clearcoatReflection=clearcoatSpecIBL*(clearcoat*0.35)*iblIntensity*topDownAtt*GLOBAL_SPECULAR_DAMP;let ao=pow(max(textureSample(aoTexture,materialSampler,uv).r,0.001),clamp(material.advanced3.y,0.0,4.0));let ambientStrength=getAmbientStrength();let ambientColor=getAmbientColor();let kD=(vec3<f32>(1.0)-F_env)*(1.0-finalMetallic)*(1.0-transmission);let ambientNormal=N_geom;let diffuseIBL=getDiffuseIndirect(ambientNormal,in.worldPos);var diffuseAmbient=kD*baseColor*(diffuseIBL*(0.5*iblIntensity)+ambientStrength*0.25+ambientColor*uShadowSettings.ambientIntensity)*ao;let hemi=mix(1.22,1.04,clamp(ambientNormal.y*0.5+0.5,0.0,1.0));let undersideLift=smoothstep(0.0,0.65,-ambientNormal.y);diffuseAmbient*=hemi*mix(1.0,1.0,undersideLift);let transmissionEnv=getDiffuseIndirect(refractDir,in.worldPos);let transmissionTerm=transmissionEnv*baseColor*transmission*(1.0-finalRoughness*0.75)*ao*iblIntensity;let metalAmbientLift=getDiffuseIndirect(ambientNormal,in.worldPos)*baseColor*finalMetallic*(0.12+ambientStrength*0.18)*ao*iblIntensity;let ambient=diffuseAmbient+reflection+clearcoatReflection+transmissionTerm+metalAmbientLift;let emissiveColor=textureSample(emissiveTexture,materialSampler,uv).rgb*material.params.z;var finalColor=directLighting+ambient+emissiveColor;let foliageLightFloor=select(0.0,0.18,alphaMaskedOpaque);finalColor=max(finalColor,baseColor*foliageLightFloor);let floorTintGate=select(0.0,1.0,inst.color.r<=0.30&&inst.color.g<=0.30&&inst.color.b<=0.32);let floorGridGate=select(0.0,1.0,isFloorLike)*floorTintGate;if(floorGridGate>0.5){let major5m=gridLineMask(in.worldPos.xz,5.0,0.050);let minor1mRaw=gridLineMask(in.worldPos.xz,1.0,0.016);let minor1m=clamp(minor1mRaw-major5m*0.9,0.0,1.0);let minorColor=vec3<f32>(0.40,0.44,0.50);let majorColor=vec3<f32>(0.78,0.81,0.86);finalColor=mix(finalColor,minorColor,minor1m*0.55);finalColor=mix(finalColor,majorColor,major5m*0.88);}let debugView=u32(material.advanced3.w);if(debugView==1u){finalColor=baseColor;}if(debugView==2u){finalColor=normalMapSample.rgb;}if(debugView==3u){finalColor=vec3<f32>(finalRoughness);}if(debugView==4u){finalColor=vec3<f32>(finalMetallic);}if(debugView==5u){finalColor=vec3<f32>(ao);}if(debugView==6u){finalColor=vec3<f32>(textureSample(heightTexture,materialSampler,uv).r);}if(debugView==7u){finalColor=vec3<f32>(fract(uv),0.0);}if(debugView==8u){finalColor=abs(normalize(N))*0.85+vec3<f32>(0.075);}finalColor=applyFog(finalColor,in.worldPos,camera.position);let outColor=vec4<f32>(finalColor,alpha);$$VELOCITY_CALC$$$$FRAGMENT_RETURN$$}\n".replace(/\$\$LIGHTING_COMMON\$\$/g,o),w=53;function C(e){let t=0;for(let a=0;a<e.length;a++)t=(t<<5)-t+e.charCodeAt(a)|0;return t>>>0}function S(e){return C(e)}function y(){return C(l)}function T(){return C(i)}function N(){return C(n)}function _(e,t){return C(I(e??{},t))}function M(e,t){let a="",r="",o="vec3<f32>(0.0, 0.0, 0.0)",l="1.0",i="",n="";return e.hasTangent&&(a+=",\n    @location(3) tangent: vec4<f32>",r+="    out.tangent = vec4<f32>(normalize(cross(out.normal, vec3<f32>(0.0, 1.0, 0.001))), 1.0);\n    ",o="in.tangent.xyz",l="in.tangent.w"),e.hasColor&&(a+=",\n    @location(4) vertexColor: vec4<f32>",r+="    out.vertexColor = vec4<f32>(1.0, 1.0, 1.0, 1.0);\n    ",i="* in.vertexColor.rgb",n="* in.vertexColor.a"),u(m(b).replace(/\$\$VERTEX_OUTPUT_EXTRA\$\$/g,a).replace(/\$\$VS_MAIN_EXTRA\$\$/g,r).replace(/\$\$PARALLAX_TANGENT\$\$/g,o).replace(/\$\$PARALLAX_TANGENT_W\$\$/g,l).replace(/\$\$VERTEX_COLOR_MUL\$\$/g,i).replace(/\$\$VERTEX_COLOR_ALPHA\$\$/g,n),t)}function L(e,t){return C(M(e??{},t))}const R="struct RigidBody{position:vec3<f32>,invMass:f32,velocity:vec3<f32>,linearDamping:f32,orientation:vec4<f32>,angularVelocity:vec3<f32>,angularDamping:f32,inverseInertia:vec3<f32>,rollingResistance:f32,restitution:f32,friction:f32,colliderID:u32,collisionFlag:u32,flags:u32,sleepCounter:u32,prevPosition:vec3<f32>,prevOrientation:vec4<f32>,collisionGroup:u32,collisionMask:u32,compliance:f32,_pad_end:f32,}struct Collider{halfExtents:vec3<f32>,shapeType:f32,}fn distanceToSegment(p:vec3<f32>,a:vec3<f32>,b:vec3<f32>)->f32{let ab=b-a;let ap=p-a;let t=clamp(dot(ap,ab)/max(dot(ab,ab),0.0001),0.0,1.0);let closest=a+t*ab;return length(p-closest);}fn quat_to_mat3(q:vec4<f32>)->mat3x3<f32>{let x=q.x;let y=q.y;let z=q.z;let w=q.w;return mat3x3<f32>(vec3<f32>(1.0-2.0*(y*y+z*z),2.0*(x*y+w*z),2.0*(x*z-w*y)),vec3<f32>(2.0*(x*y-w*z),1.0-2.0*(x*x+z*z),2.0*(y*z+w*x)),vec3<f32>(2.0*(x*z+w*y),2.0*(y*z-w*x),1.0-2.0*(x*x+y*y)));}struct Camera{viewProjection:mat4x4<f32>,position:vec3<f32>,padding:f32,prevViewProjection:mat4x4<f32>,};@group(0)@binding(0)var<uniform>camera:Camera;$$LIGHTING_COMMON$$@group(0)@binding(4)var envMap:texture_2d<f32>;fn sampleEquirectangular(dir:vec3<f32>)->vec3<f32>{let n=normalize(dir);let lon=atan2(n.z,n.x);let lat=asin(clamp(n.y,-1.0,1.0));let u=fract(lon/6.28318530718+0.5);let v=0.5-(lat/3.14159265359);return textureSample(envMap,iblSampler,vec2<f32>(u,clamp(v,0.0,1.0))).rgb;}struct MaterialUniforms{albedo:vec4<f32>,params:vec4<f32>,};@group(2)@binding(0)var<uniform>material:MaterialUniforms;@group(2)@binding(1)var albedoTexture:texture_2d<f32>;@group(2)@binding(2)var normalTexture:texture_2d<f32>;@group(2)@binding(3)var metallicRoughnessTexture:texture_2d<f32>;@group(2)@binding(4)var aoTexture:texture_2d<f32>;@group(2)@binding(5)var emissiveTexture:texture_2d<f32>;@group(2)@binding(6)var heightTexture:texture_2d<f32>;@group(2)@binding(7)var materialSampler:sampler;@group(2)@binding(8)var reflectionTexture:texture_2d<f32>;@group(2)@binding(9)var reflectionSampler:sampler;struct ReflectionCameraData{matrix:mat4x4<f32>}@group(2)@binding(10)var<uniform>reflectionCamera:ReflectionCameraData;@group(3)@binding(0)var<storage,read>bodies:array<RigidBody>;@group(3)@binding(2)var<storage,read>instanceIndices:array<u32>;@group(3)@binding(3)var<storage,read>colliders:array<Collider>;struct BatchUniform{color:vec4<f32>,receiveShadows:f32,alphaCutoff:f32,debugPhysics:f32,showStripes:f32,sphereRadiusOverride:f32,};@group(3)@binding(1)var<uniform>batch:BatchUniform;struct VertexInput{@location(0)position:vec3<f32>,@location(1)normal:vec3<f32>,@location(2)uv:vec2<f32>};struct VertexOutput{@builtin(position)position:vec4<f32>,@location(0)worldPos:vec3<f32>,@location(1)normal:vec3<f32>,@location(2)uv:vec2<f32>,@location(5)@interpolate(flat)instanceIndex:u32,@location(6)@interpolate(flat)collisionFlag:u32,@location(7)localPos:vec3<f32>,@location(8)@interpolate(flat)cornerContact:u32};const GLOBAL_SPECULAR_DAMP:f32=0.28;@vertex fn vs_main(input:VertexInput,@builtin(instance_index)instanceIndex:u32)->VertexOutput{var out:VertexOutput;let realIndex=instanceIndices[instanceIndex];let body=bodies[realIndex];let R=quat_to_mat3(body.orientation);var localPos=input.position;var localNormal=input.normal;let cid=body.colliderID;if(cid<arrayLength(&colliders)){let c=colliders[cid];if(c.shapeType<0.5||batch.sphereRadiusOverride>0.01){let r=select(max(c.halfExtents.x,0.01),batch.sphereRadiusOverride,batch.sphereRadiusOverride>0.01);let len=length(input.position);if(len>0.001){localPos=normalize(input.position)*r;localNormal=normalize(input.position);}}else if(c.shapeType>=0.5&&c.shapeType<2.5){let hx=max(c.halfExtents.x,0.01);let hy=max(c.halfExtents.y,0.01);let hz=select(hx,max(c.halfExtents.z,0.01),c.halfExtents.z>0.01);localPos=localPos*vec3<f32>(2.0*hx,2.0*hy,2.0*hz);}}let modelMatrix=mat4x4<f32>(vec4<f32>(R[0],0.0),vec4<f32>(R[1],0.0),vec4<f32>(R[2],0.0),vec4<f32>(body.position.xyz,1.0));let worldPos4=modelMatrix*vec4<f32>(localPos,1.0);out.position=camera.viewProjection*worldPos4;out.worldPos=worldPos4.xyz;out.normal=normalize(R*localNormal);out.uv=input.uv;out.instanceIndex=realIndex;out.collisionFlag=body.collisionFlag;out.localPos=localPos;let cx=select(0u,1u,input.position.x>0.0);let cy=select(0u,2u,input.position.y>0.0);let cz=select(0u,4u,input.position.z>0.0);let cornerIdx=cx+cy+cz;out.cornerContact=select(0u,1u,(body.collisionFlag&(1u<<cornerIdx))!=0u);return out;}fn fresnelSchlick(cosTheta:f32,F0:vec3<f32>)->vec3<f32>{return F0+(vec3<f32>(1.0)-F0)*pow(clamp(1.0-cosTheta,0.0,1.0),5.0);}fn distributionGGX(NdotH:f32,roughness:f32)->f32{let a=roughness*roughness;let a2=a*a;let NdotH2=NdotH*NdotH;return(a2)/max(3.14159265359*(NdotH2*(a2-1.0)+1.0)*(NdotH2*(a2-1.0)+1.0),0.0000001);}fn geometrySchlickGGX(NdotV:f32,roughness:f32)->f32{let r=(roughness+1.0);let k=(r*r)/8.0;return NdotV/max(NdotV*(1.0-k)+k,0.0000001);}fn geometrySmith(NdotV:f32,NdotL:f32,roughness:f32)->f32{return geometrySchlickGGX(NdotV,roughness)*geometrySchlickGGX(NdotL,roughness);}fn envBRDFApprox(F0:vec3<f32>,roughness:f32,NdotV:f32)->vec3<f32>{let c0=vec4<f32>(-1.0,-0.0275,-0.572,0.022);let c1=vec4<f32>(1.0,0.0425,1.04,-0.04);let r=roughness*c0+c1;let a004=min(r.x*r.x,exp2(-9.28*NdotV))*r.x+r.y;let ab=vec2<f32>(-1.04,1.04)*a004+r.zw;return F0*ab.x+vec3<f32>(ab.y);}fn cookTorranceBRDF(N:vec3<f32>,V:vec3<f32>,L:vec3<f32>,albedo:vec3<f32>,metallic:f32,roughness:f32)->vec3<f32>{let H=normalize(V+L);let NdotV=max(dot(N,V),0.0000001);let NdotL=max(dot(N,L),0.0000001);let NdotH=max(dot(N,H),0.0);let VdotH=max(dot(V,H),0.0);let F0=mix(vec3<f32>(0.04),albedo,metallic);let F=fresnelSchlick(VdotH,F0);let D=distributionGGX(NdotH,roughness);let G=geometrySmith(NdotV,NdotL,roughness);let numerator=D*G*F;let denominator=4.0*NdotV*NdotL;let specular=min(numerator/max(denominator,0.0000001),vec3<f32>(4.0));let kD=(vec3<f32>(1.0)-F)*(1.0-metallic);return(kD*albedo/3.14159265359+specular*GLOBAL_SPECULAR_DAMP)*NdotL;}$$VELOCITY_FRAGMENT_STRUCT$$@fragment fn fs_main(in:VertexOutput,@builtin(front_facing)_front_facing:bool)->$$FRAGMENT_RETURN_TYPE$${let N_geom=normalize(in.normal);var N=N_geom;let V=normalize(camera.position-in.worldPos);let uv=in.uv;var albedoTexColor=textureSample(albedoTexture,materialSampler,uv);if(batch.showStripes>0.5){let stripe=floor(uv.x*8.0);let stripeOn=select(0.0,1.0,stripe-floor(stripe*0.5)*2.0<0.5);let stripeColor=mix(vec3<f32>(0.15,0.15,0.2),vec3<f32>(0.98,0.55,0.45),stripeOn);albedoTexColor=vec4<f32>(stripeColor,1.0);}let baseColor=albedoTexColor.rgb*material.albedo.rgb*batch.color.rgb;var alpha=albedoTexColor.a*material.albedo.a*batch.color.a;if(batch.alphaCutoff>0.0){if(alpha<batch.alphaCutoff){discard;}alpha=1.0;}let mrSample=textureSample(metallicRoughnessTexture,materialSampler,uv);let finalMetallic=material.params.x*mrSample.b;let finalRoughness=clamp(material.params.y*mrSample.g,0.22,1.0);let normalMapSample=textureSample(normalTexture,materialSampler,uv);if(length(normalMapSample.rgb)>0.01){let normalMapTS=normalMapSample.rgb*2.0-vec3<f32>(1.0);var rawT=cross(N_geom,vec3<f32>(0.0,1.0,0.0));if(length(rawT)<0.0001){rawT=cross(N_geom,vec3<f32>(1.0,0.0,0.0));}if(length(rawT)<0.0001){rawT=cross(N_geom,vec3<f32>(0.0,0.0,1.0));}let T=normalize(rawT);let B=cross(N_geom,T);let TBN=mat3x3<f32>(T,B,N_geom);N=normalize(TBN*normalMapTS);}var directLighting=vec3<f32>(0.0);let lightCount=getLightCount();for(var i=0;i<MAX_LIGHTS;i++){if(i>=lightCount){break;}let light=getLightAt(i);let lightData=getLightDirectionAndDistance(light,in.worldPos);let L=lightData.xyz;let lightDistance=lightData.w;let recvShadows=batch.receiveShadows;let applyShadow=shouldApplyShadowForLight(i,light.direction.w);var radiance=getLightRadiance(i,light,in.worldPos,N_geom,recvShadows,L,lightDistance,applyShadow,1.0)*2.0;directLighting+=cookTorranceBRDF(N,V,L,baseColor,finalMetallic,finalRoughness)*radiance;}let R=reflect(-V,N);let NdotV=max(dot(N,V),0.0);let F0=mix(vec3<f32>(0.04),baseColor,finalMetallic);let F_env=fresnelSchlick(NdotV,F0);let reflectionStrength=(1.0-finalRoughness)*max(finalMetallic,0.04);let worldPos4=vec4<f32>(in.worldPos,1.0);let reflectionNDC=(reflectionCamera.matrix*worldPos4).xyz/(reflectionCamera.matrix*worldPos4).w;var reflectionUV=clamp(vec2<f32>(reflectionNDC.x*0.5+0.5,-reflectionNDC.y*0.5+0.5),vec2<f32>(0.0),vec2<f32>(1.0));let reflectionColor=textureSample(reflectionTexture,reflectionSampler,reflectionUV);let specularIBL=getSpecularIBL(R,finalRoughness,NdotV,F0);let iblIntensity=getIblIntensity();let topDownAtt=getTopDownSpecularAttenuation(V);var reflection=specularIBL*reflectionStrength*iblIntensity*topDownAtt*GLOBAL_SPECULAR_DAMP;let ao=clamp(textureSample(aoTexture,materialSampler,uv).r,0.9,1.0);let ambientStrength=getAmbientStrength();let ambientColor=getAmbientColor();let kD=(vec3<f32>(1.0)-F_env)*(1.0-finalMetallic);let diffuseIBL=getDiffuseIndirect(N_geom,in.worldPos);let diffuseAmbient=kD*baseColor*(diffuseIBL*(0.5*iblIntensity)+ambientStrength*0.25+ambientColor*uShadowSettings.ambientIntensity)*ao;let ambient=diffuseAmbient+reflection;let emissiveColor=textureSample(emissiveTexture,materialSampler,uv).rgb*material.params.z;var finalColor=directLighting+ambient+emissiveColor;finalColor=max(finalColor,baseColor*0.045);let debugCollisionPoints=in.collisionFlag;let bodyInContact=batch.debugPhysics>0.5&&debugCollisionPoints!=0u;let body=bodies[in.instanceIndex];let bodyR=quat_to_mat3(body.orientation);let cid=body.colliderID;var hx=0.5;var hy=0.5;var hz=0.5;if(cid<arrayLength(&colliders)){let c=colliders[cid];hx=max(c.halfExtents.x,0.01);hy=max(c.halfExtents.y,0.01);hz=select(max(c.halfExtents.x,0.01),max(c.halfExtents.z,0.01),c.halfExtents.z>0.01);}let corners=array<vec3<f32>,8>(vec3<f32>(-hx,-hy,-hz),vec3<f32>(hx,-hy,-hz),vec3<f32>(-hx,hy,-hz),vec3<f32>(hx,hy,-hz),vec3<f32>(-hx,-hy,hz),vec3<f32>(hx,-hy,hz),vec3<f32>(-hx,hy,hz),vec3<f32>(hx,hy,hz));var contactCorners:array<vec3<f32>,2>;var contactCount=0u;for(var i=0u;i<8u;i++){if(contactCount<2u&&(debugCollisionPoints&(1u<<i))!=0u){contactCorners[contactCount]=body.position+bodyR*corners[i];contactCount+=1u;}}let edgeRadius=0.08;var nearContactEdge=false;if(contactCount==2u){let d=distanceToSegment(in.worldPos,contactCorners[0],contactCorners[1]);nearContactEdge=d<edgeRadius;}else if(contactCount==1u){nearContactEdge=length(in.worldPos-contactCorners[0])<edgeRadius;}else{nearContactEdge=in.localPos.y<-0.35;}let showContactHighlight=bodyInContact&&nearContactEdge;let brightRed=vec3<f32>(1.0,0.0,0.0);let contactCornerRed=select(finalColor,brightRed,showContactHighlight);let debugColor=select(finalColor,contactCornerRed,bodyInContact);let foggedColor=applyFog(debugColor,in.worldPos,camera.position);let outColor=vec4<f32>(foggedColor,alpha);$$VELOCITY_CALC$$$$FRAGMENT_RETURN$$}\n".replace(/\$\$LIGHTING_COMMON\$\$/g,o);function $(e,t){let a="",r="",o="",l="vec3<f32>(0.0, 0.0, 0.0)",i="1.0",n="",s="";return e.hasTangent&&(a+=",\n    @location(3) tangent: vec4<f32>",r+=",\n    @location(3) tangent: vec4<f32>",o+="    out.tangent = vec4<f32>(normalize(input.tangent.xyz), input.tangent.w);\n    ",l="in.tangent.xyz",i="in.tangent.w"),e.hasColor&&(a+=",\n    @location(4) color: vec4<f32>",r+=",\n    @location(4) vertexColor: vec4<f32>",o+="out.vertexColor = input.color;\n    ",n="* in.vertexColor.rgb",s="* in.vertexColor.a"),u(m(v).replace(/\$\$VERTEX_INPUT_EXTRA\$\$/g,a).replace(/\$\$VERTEX_OUTPUT_EXTRA\$\$/g,r).replace(/\$\$VS_MAIN_EXTRA\$\$/g,o).replace(/\$\$PARALLAX_TANGENT\$\$/g,l).replace(/\$\$PARALLAX_TANGENT_W\$\$/g,i).replace(/\$\$VERTEX_COLOR_MUL\$\$/g,n).replace(/\$\$VERTEX_COLOR_ALPHA\$\$/g,s),t)}function I(e,t){let a="",r="",o="",l="",i="",n="",s="vec3<f32>(0.0, 0.0, 0.0)",c="1.0",f="",d="";return e.hasTangent&&(a+=",\n    @location(3) tangent: vec4<f32>",r+=",\n    @location(3) tangent: vec4<f32>",l="var tanPreWorld = input.tangent.xyz;\n    var tanHandW = input.tangent.w;\n    ",i="tanPreWorld = rotateEulerXYZ(input.tangent.xyz, angles);\n        ",n="tanPreWorld = rotateEulerXYZ(input.tangent.xyz, vec3<f32>(swayRx, 0.0, swayRz));\n        ",o+="    {\n        let rotForTangent = mat3x3<f32>(inst.worldMatrix[0].xyz, inst.worldMatrix[1].xyz, inst.worldMatrix[2].xyz);\n        out.tangent = vec4<f32>(normalize(rotForTangent * tanPreWorld), tanHandW);\n    }\n    ",s="in.tangent.xyz",c="in.tangent.w"),e.hasColor&&(a+=",\n    @location(4) color: vec4<f32>",r+=",\n    @location(4) vertexColor: vec4<f32>",o+="out.vertexColor = input.color;\n    ",f="* in.vertexColor.rgb",d="* in.vertexColor.a"),u(m(g).replace(/\$\$VERTEX_INPUT_EXTRA\$\$/g,a).replace(/\$\$VERTEX_OUTPUT_EXTRA\$\$/g,r).replace(/\$\$INSTANCED_TAN_DECL\$\$/g,l).replace(/\$\$TAN_FIX_EULER\$\$/g,i).replace(/\$\$TAN_FIX_WIND\$\$/g,n).replace(/\$\$VS_MAIN_EXTRA\$\$/g,o).replace(/\$\$PARALLAX_TANGENT\$\$/g,s).replace(/\$\$PARALLAX_TANGENT_W\$\$/g,c).replace(/\$\$VERTEX_COLOR_MUL\$\$/g,f).replace(/\$\$VERTEX_COLOR_ALPHA\$\$/g,d),t)}function z(e){let t="";return e.hasTangent&&(t+=",\n    @location(3) tangent: vec4<f32>"),e.hasColor&&(t+=",\n    @location(4) color: vec4<f32>"),t}function P(e){const t=z(e);return A.replace(/\$\$BLOOM_VERTEX_INPUT_EXTRA\$\$/g,t)}function V(e){const t=z(e);return d.replace(/\$\$BLOOM_VERTEX_INPUT_EXTRA\$\$/g,t)}const A=f,E="struct Camera{viewProjection:mat4x4<f32>,position:vec3<f32>,padding:f32,};@group(0)@binding(0)var<uniform>camera:Camera;struct ModelUniform{matrix:mat4x4<f32>,color:vec4<f32>,receiveShadows:f32,alphaCutoff:f32,shadowReceiverBiasMul:f32,padding:f32,};@group(2)@binding(0)var<uniform>model:ModelUniform;struct VertexInput{@location(0)position:vec3<f32>,@location(1)normal:vec3<f32>,@location(2)uv:vec2<f32>$$WIREFRAME_VERTEX_INPUT_EXTRA$$};struct VertexOutput{@builtin(position)position:vec4<f32>,};@vertex fn vs_main(input:VertexInput)->VertexOutput{var out:VertexOutput;let worldPos4=model.matrix*vec4<f32>(input.position,1.0);out.position=camera.viewProjection*worldPos4;return out;}@fragment fn fs_main(_in:VertexOutput)->@location(0)vec4<f32>{return vec4<f32>(0.0,0.0,0.0,1.0);}\n";class D{static getShader(e,t,a,r){const o=t;let l=this.shaderCache.get(o);l||(l=new Map,this.shaderCache.set(o,l));const i=a??{},n=i.hasTangent||i.hasColor?`_${i.hasTangent?"t":""}${i.hasColor?"c":""}`:"";let s;switch(e){case"standard":default:s=$(i,r);break;case"unlit":s="struct Camera{viewProjection:mat4x4<f32>,position:vec3<f32>,padding:f32,};@group(0)@binding(0)var<uniform>camera:Camera;struct ModelUniform{matrix:mat4x4<f32>,color:vec4<f32>,receiveShadows:f32,alphaCutoff:f32,shadowReceiverBiasMul:f32,padding:f32,};@group(2)@binding(0)var<uniform>model:ModelUniform;@group(2)@binding(1)var albedoTexture:texture_2d<f32>;@group(2)@binding(2)var materialSampler:sampler;struct VertexInput{@location(0)position:vec3<f32>,@location(1)normal:vec3<f32>,@location(2)uv:vec2<f32>,};struct VertexOutput{@builtin(position)position:vec4<f32>,@location(0)uv:vec2<f32>,};@vertex fn vs_main(input:VertexInput)->VertexOutput{var out:VertexOutput;let worldPos4=model.matrix*vec4<f32>(input.position,1.0);out.position=camera.viewProjection*worldPos4;out.uv=input.uv;return out;}@fragment fn fs_main(in:VertexOutput)->@location(0)vec4<f32>{let color=textureSample(albedoTexture,materialSampler,in.uv)*model.color;if(model.alphaCutoff>0.0&&color.a<model.alphaCutoff){discard;}return color;}\n";break;case"emissive":s="struct Camera{viewProjection:mat4x4<f32>,position:vec3<f32>,padding:f32,};@group(0)@binding(0)var<uniform>camera:Camera;struct ModelUniform{matrix:mat4x4<f32>,color:vec4<f32>,emissive:vec3<f32>,emissiveIntensity:f32,};@group(2)@binding(0)var<uniform>model:ModelUniform;@group(2)@binding(1)var albedoTexture:texture_2d<f32>;@group(2)@binding(2)var materialSampler:sampler;struct VertexInput{@location(0)position:vec3<f32>,@location(1)normal:vec3<f32>,@location(2)uv:vec2<f32>,};struct VertexOutput{@builtin(position)position:vec4<f32>,@location(0)uv:vec2<f32>,};@vertex fn vs_main(input:VertexInput)->VertexOutput{var out:VertexOutput;let worldPos4=model.matrix*vec4<f32>(input.position,1.0);out.position=camera.viewProjection*worldPos4;out.uv=input.uv;return out;}@fragment fn fs_main(in:VertexOutput)->@location(0)vec4<f32>{let baseColor=textureSample(albedoTexture,materialSampler,in.uv)*model.color;if(baseColor.a<0.1){discard;}let emissive=model.emissive*model.emissiveIntensity;let finalColor=baseColor.rgb+emissive;return vec4<f32>(finalColor,baseColor.a);}\n";break;case"bloom_extract":s=P(i)}const c=C(s),f=String(e)+n+p(r)+`_h${c}`,d=l.get(f);if(d)return d;const u=o.createShaderModule({code:s});return l.set(f,u),u}static getBloomExtractInstancedShader(e,t){const a=e;let r=this.shaderCache.get(a);r||(r=new Map,this.shaderCache.set(a,r));const o=t??{},l=o.hasTangent||o.hasColor?`_${o.hasTangent?"t":""}${o.hasColor?"c":""}`:"",i=V(o),n="bloom_extract_instanced"+l+`_h${C(i)}`,s=r.get(n);if(s)return s;const c=a.createShaderModule({code:i});return r.set(n,c),c}static getShaderInstanced(e,t,a){const r=e;let o=this.shaderCache.get(r);o||(o=new Map,this.shaderCache.set(r,o));const l=t??{},i=l.hasTangent||l.hasColor?`_${l.hasTangent?"t":""}${l.hasColor?"c":""}`:"",n=I(l,a),s=`standard_instanced_v65_h${C(n)}`+i+p(a),c=o.get(s);if(c)return c;const f=r.createShaderModule({code:n});return o.set(s,f),f}static getMeshletExpandedInstancedShader(e,t,a){const r=e;let o=this.shaderCache.get(r);o||(o=new Map,this.shaderCache.set(r,o));const l=t??{},i=l.hasTangent||l.hasColor?`_${l.hasTangent?"t":""}${l.hasColor?"c":""}`:"",n=M(l,a),s=`meshlet_expanded_instanced_v53_h${C(n)}`+i+p(a),c=o.get(s);if(c)return c;const f=r.createShaderModule({code:n});return o.set(s,f),f}static getShadowShader(e,t){const r=t;let o,i=this.shadowShaderCache.get(r);i||(i=new Map,this.shadowShaderCache.set(r,i)),o="standard"===e||"lit"===e?l:a("\n            $$SHADOW_ABI$$\n            struct ModelUniform {\n                matrix: mat4x4<f32>,\n                color: vec4<f32>,\n                receiveShadows: f32,\n                alphaCutoff: f32,\n                padding: vec2<f32>,\n            };\n            @group(1) @binding(0) var<uniform> model: ModelUniform;\n            struct VertexInput { @location(0) position: vec3<f32>, };\n            @vertex\n            fn vs_shadow(input: VertexInput) -> @builtin(position) vec4<f32> {\n                let worldPos4 = model.matrix * vec4<f32>(input.position, 1.0);\n                let matrixIndex = i32(min(uShadowPass.matrixIndex, 23u));\n                return uShadowMatrices.matrices[matrixIndex] * worldPos4;\n            }\n            ");const n=`${String(e)}_h${C(o)}`,s=i.get(n);if(s)return s;const c=r.createShaderModule({code:o});return i.set(n,c),c}static getPhysicsInstancedShader(e,t){const a=e;let r=this.shaderCache.get(a);r||(r=new Map,this.shaderCache.set(a,r));const o="physics_instanced"+p(t),l=r.get(o);if(l)return l;const i=a.createShaderModule({code:u(R,t)});return r.set(o,i),i}static getPhysicsShadowShaderInstanced(e){const t=e;let a=this.shaderCache.get(t);a||(a=new Map,this.shaderCache.set(t,a));const r="physics_shadow_instanced",o=a.get(r);if(o)return o;const l=t.createShaderModule({code:s});return a.set(r,l),l}static getForgePhysicsInstancedShader(e,t){const a=e;let r=this.shaderCache.get(a);r||(r=new Map,this.shaderCache.set(a,r));const l="forge_physics_instanced"+p(t),i=r.get(l);if(i)return i;const n=u("struct RigidBodyData{position:vec3f,pad0:f32,orientation:vec4f,linearVelocity:vec3f,pad1:f32,angularVelocity:vec3f,pad2:f32,invMass:f32,invInertiaX:f32,invInertiaY:f32,invInertiaZ:f32,friction:f32,rollingResistance:f32,sleepState:u32,sleepTimer:f32,dominance:u32,pad4:u32,pad5:u32,pad6:u32,}struct ColliderData{shapeType:u32,radius:f32,height:f32,pad0:u32,halfExtentsX:f32,halfExtentsY:f32,halfExtentsZ:f32,pad1:u32,}fn quat_to_mat3(q:vec4<f32>)->mat3x3<f32>{let x=q.x;let y=q.y;let z=q.z;let w=q.w;return mat3x3<f32>(vec3<f32>(1.0-2.0*(y*y+z*z),2.0*(x*y+w*z),2.0*(x*z-w*y)),vec3<f32>(2.0*(x*y-w*z),1.0-2.0*(x*x+z*z),2.0*(y*z+w*x)),vec3<f32>(2.0*(x*z+w*y),2.0*(y*z-w*x),1.0-2.0*(x*x+y*y)));}struct Camera{viewProjection:mat4x4<f32>,position:vec3<f32>,padding:f32,prevViewProjection:mat4x4<f32>,};@group(0)@binding(0)var<uniform>camera:Camera;$$LIGHTING_COMMON$$@group(0)@binding(4)var envMap:texture_2d<f32>;fn sampleEquirectangular(dir:vec3<f32>)->vec3<f32>{let n=normalize(dir);let lon=atan2(n.z,n.x);let lat=asin(clamp(n.y,-1.0,1.0));let u=fract(lon/6.28318530718+0.5);let v=0.5-(lat/3.14159265359);return textureSample(envMap,iblSampler,vec2<f32>(u,clamp(v,0.0,1.0))).rgb;}struct MaterialUniforms{albedo:vec4<f32>,params:vec4<f32>,};@group(2)@binding(0)var<uniform>material:MaterialUniforms;@group(2)@binding(1)var albedoTexture:texture_2d<f32>;@group(2)@binding(2)var normalTexture:texture_2d<f32>;@group(2)@binding(3)var metallicRoughnessTexture:texture_2d<f32>;@group(2)@binding(4)var aoTexture:texture_2d<f32>;@group(2)@binding(5)var emissiveTexture:texture_2d<f32>;@group(2)@binding(6)var heightTexture:texture_2d<f32>;@group(2)@binding(7)var materialSampler:sampler;@group(2)@binding(8)var reflectionTexture:texture_2d<f32>;@group(2)@binding(9)var reflectionSampler:sampler;struct ReflectionCameraData{matrix:mat4x4<f32>}@group(2)@binding(10)var<uniform>reflectionCamera:ReflectionCameraData;@group(3)@binding(0)var<storage,read>bodies:array<RigidBodyData>;@group(3)@binding(2)var<storage,read>instanceIndices:array<u32>;@group(3)@binding(3)var<storage,read>colliders:array<ColliderData>;@group(3)@binding(4)var<storage,read>prevBodies:array<RigidBodyData>;struct BatchUniform{color:vec4<f32>,receiveShadows:f32,alphaCutoff:f32,debugPhysics:f32,showStripes:f32,sphereRadiusOverride:f32,physicsAlpha:f32,perInstanceHueTint:f32,};@group(3)@binding(1)var<uniform>batch:BatchUniform;struct VertexInput{@location(0)position:vec3<f32>,@location(1)normal:vec3<f32>,@location(2)uv:vec2<f32>};struct VertexOutput{@builtin(position)position:vec4<f32>,@location(0)worldPos:vec3<f32>,@location(1)normal:vec3<f32>,@location(2)uv:vec2<f32>,@location(5)@interpolate(flat)instanceIndex:u32,@location(6)@interpolate(flat)sleepState:u32,@location(7)localPos:vec3<f32>,@location(8)@interpolate(flat)cornerContact:u32};const GLOBAL_SPECULAR_DAMP:f32=0.28;fn quat_nlerp(a:vec4<f32>,b:vec4<f32>,t:f32)->vec4<f32>{var b_=b;if(dot(a,b)<0.0){b_=-b;}return normalize(mix(a,b_,t));}@vertex fn vs_main(input:VertexInput,@builtin(instance_index)instanceIndex:u32)->VertexOutput{var out:VertexOutput;let realIndex=instanceIndices[instanceIndex];let body=bodies[realIndex];let alpha=batch.physicsAlpha;var pos=body.position;var orient=body.orientation;if(realIndex<arrayLength(&prevBodies)&&alpha<0.999){let prev=prevBodies[realIndex];pos=mix(prev.position,body.position,alpha);orient=quat_nlerp(prev.orientation,body.orientation,alpha);}let R=quat_to_mat3(orient);var localPos=input.position;var localNormal=input.normal;if(realIndex<arrayLength(&colliders)){let c=colliders[realIndex];let hx=max(c.halfExtentsX,0.01);let hy=max(c.halfExtentsY,0.01);let hz=max(c.halfExtentsZ,0.01);let r=max(c.radius,0.01);if(batch.sphereRadiusOverride>0.01){let rad=batch.sphereRadiusOverride;let len=length(input.position);if(len>0.001){localPos=normalize(input.position)*rad;localNormal=normalize(input.position);}}else if(c.shapeType==0u){let len=length(input.position);if(len>0.001){localPos=normalize(input.position)*r;localNormal=normalize(input.position);}}else if(c.shapeType==1u){localPos=localPos*vec3<f32>(2.0*hx,2.0*hy,2.0*hz);}else if(c.shapeType==4u){localPos=localPos*vec3<f32>(2.0*hx,2.0*hy,2.0*hz);localPos=localPos+vec3<f32>(0.0,hy*0.5,0.0);}else if(c.shapeType==2u){let len=length(input.position);if(len>0.001){localPos=normalize(input.position)*r;localNormal=normalize(input.position);}}}let modelMatrix=mat4x4<f32>(vec4<f32>(R[0],0.0),vec4<f32>(R[1],0.0),vec4<f32>(R[2],0.0),vec4<f32>(pos.xyz,1.0));let worldPos4=modelMatrix*vec4<f32>(localPos,1.0);out.position=camera.viewProjection*worldPos4;out.worldPos=worldPos4.xyz;out.normal=normalize(R*localNormal);out.uv=input.uv;out.instanceIndex=realIndex;out.sleepState=body.sleepState;out.localPos=localPos;out.cornerContact=0u;return out;}fn fresnelSchlick(cosTheta:f32,F0:vec3<f32>)->vec3<f32>{return F0+(vec3<f32>(1.0)-F0)*pow(clamp(1.0-cosTheta,0.0,1.0),5.0);}fn distributionGGX(NdotH:f32,roughness:f32)->f32{let a=roughness*roughness;let a2=a*a;let NdotH2=NdotH*NdotH;return(a2)/max(3.14159265359*(NdotH2*(a2-1.0)+1.0)*(NdotH2*(a2-1.0)+1.0),0.0000001);}fn geometrySchlickGGX(NdotV:f32,roughness:f32)->f32{let r=(roughness+1.0);let k=(r*r)/8.0;return NdotV/max(NdotV*(1.0-k)+k,0.0000001);}fn geometrySmith(NdotV:f32,NdotL:f32,roughness:f32)->f32{return geometrySchlickGGX(NdotV,roughness)*geometrySchlickGGX(NdotL,roughness);}fn envBRDFApprox(F0:vec3<f32>,roughness:f32,NdotV:f32)->vec3<f32>{let c0=vec4<f32>(-1.0,-0.0275,-0.572,0.022);let c1=vec4<f32>(1.0,0.0425,1.04,-0.04);let r=roughness*c0+c1;let a004=min(r.x*r.x,exp2(-9.28*NdotV))*r.x+r.y;let ab=vec2<f32>(-1.04,1.04)*a004+r.zw;return F0*ab.x+vec3<f32>(ab.y);}fn forge_hash01(ix:u32,salt:f32)->f32{return fract(sin(f32(ix)*12.9898+salt)*43758.5453);}fn hsv_to_rgb(h:f32,s:f32,v:f32)->vec3<f32>{let hh=fract(h)*6.0;let i=i32(floor(hh));let f=hh-f32(i);let p=v*(1.0-s);let q=v*(1.0-s*f);let t=v*(1.0-s*(1.0-f));switch i{case 0:{return vec3<f32>(v,t,p);}case 1:{return vec3<f32>(q,v,p);}case 2:{return vec3<f32>(p,v,t);}case 3:{return vec3<f32>(p,q,v);}case 4:{return vec3<f32>(t,p,v);}default:{return vec3<f32>(v,p,q);}}}fn forge_instance_hue_rgb(body_ix:u32)->vec3<f32>{let h=forge_hash01(body_ix,0.17);let s=0.46+0.48*forge_hash01(body_ix,4.2);let vv=0.66+0.30*forge_hash01(body_ix,8.9);return hsv_to_rgb(h,s,vv);}fn cookTorranceBRDF(N:vec3<f32>,V:vec3<f32>,L:vec3<f32>,albedo:vec3<f32>,metallic:f32,roughness:f32)->vec3<f32>{let H=normalize(V+L);let NdotV=max(dot(N,V),0.0000001);let NdotL=max(dot(N,L),0.0000001);let NdotH=max(dot(N,H),0.0);let VdotH=max(dot(V,H),0.0);let F0=mix(vec3<f32>(0.04),albedo,metallic);let F=fresnelSchlick(VdotH,F0);let D=distributionGGX(NdotH,roughness);let G=geometrySmith(NdotV,NdotL,roughness);let numerator=D*G*F;let denominator=4.0*NdotV*NdotL;let specular=min(numerator/max(denominator,0.0000001),vec3<f32>(4.0));let kD=(vec3<f32>(1.0)-F)*(1.0-metallic);return(kD*albedo/3.14159265359+specular*GLOBAL_SPECULAR_DAMP)*NdotL;}$$VELOCITY_FRAGMENT_STRUCT$$@fragment fn fs_main(in:VertexOutput,@builtin(front_facing)_front_facing:bool)->$$FRAGMENT_RETURN_TYPE$${let N_geom=normalize(in.normal);var N=N_geom;let V=normalize(camera.position-in.worldPos);let uv=in.uv;var albedoTexColor=textureSample(albedoTexture,materialSampler,uv);if(batch.showStripes>0.5){let stripe=floor(uv.x*8.0);let stripeOn=select(0.0,1.0,stripe-floor(stripe*0.5)*2.0<0.5);let stripeColor=mix(vec3<f32>(0.15,0.15,0.2),vec3<f32>(0.98,0.55,0.45),stripeOn);albedoTexColor=vec4<f32>(stripeColor,1.0);}var tint=vec3<f32>(1.0);if(batch.perInstanceHueTint>0.5){tint=forge_instance_hue_rgb(in.instanceIndex);}let baseColor=albedoTexColor.rgb*material.albedo.rgb*batch.color.rgb*tint;var alpha=albedoTexColor.a*material.albedo.a*batch.color.a;if(batch.alphaCutoff>0.0){if(alpha<batch.alphaCutoff){discard;}alpha=1.0;}let mrSample=textureSample(metallicRoughnessTexture,materialSampler,uv);let finalMetallic=material.params.x*mrSample.b;let finalRoughness=clamp(material.params.y*mrSample.g,0.22,1.0);let normalMapSample=textureSample(normalTexture,materialSampler,uv);if(length(normalMapSample.rgb)>0.01){let normalMapTS=normalMapSample.rgb*2.0-vec3<f32>(1.0);var rawT=cross(N_geom,vec3<f32>(0.0,1.0,0.0));if(length(rawT)<0.0001){rawT=cross(N_geom,vec3<f32>(1.0,0.0,0.0));}if(length(rawT)<0.0001){rawT=cross(N_geom,vec3<f32>(0.0,0.0,1.0));}let T=normalize(rawT);let B=cross(N_geom,T);let TBN=mat3x3<f32>(T,B,N_geom);N=normalize(TBN*normalMapTS);}var directLighting=vec3<f32>(0.0);let lightCount=getLightCount();for(var i=0;i<MAX_LIGHTS;i++){if(i>=lightCount){break;}let light=getLightAt(i);let lightData=getLightDirectionAndDistance(light,in.worldPos);let L=lightData.xyz;let lightDistance=lightData.w;let recvShadows=batch.receiveShadows;let applyShadow=shouldApplyShadowForLight(i,light.direction.w);var radiance=getLightRadiance(i,light,in.worldPos,N_geom,recvShadows,L,lightDistance,applyShadow,1.0)*2.0;directLighting+=cookTorranceBRDF(N,V,L,baseColor,finalMetallic,finalRoughness)*radiance;}let R=reflect(-V,N);let NdotV=max(dot(N,V),0.0);let F0=mix(vec3<f32>(0.04),baseColor,finalMetallic);let F_env=fresnelSchlick(NdotV,F0);let reflectionStrength=(1.0-finalRoughness)*max(finalMetallic,0.04);let worldPos4=vec4<f32>(in.worldPos,1.0);let reflectionNDC=(reflectionCamera.matrix*worldPos4).xyz/(reflectionCamera.matrix*worldPos4).w;var reflectionUV=clamp(vec2<f32>(reflectionNDC.x*0.5+0.5,-reflectionNDC.y*0.5+0.5),vec2<f32>(0.0),vec2<f32>(1.0));let reflectionColor=textureSample(reflectionTexture,reflectionSampler,reflectionUV);let specularIBL=getSpecularIBL(R,finalRoughness,NdotV,F0);let iblIntensity=getIblIntensity();let topDownAtt=getTopDownSpecularAttenuation(V);var reflection=specularIBL*reflectionStrength*iblIntensity*topDownAtt*GLOBAL_SPECULAR_DAMP;let ao=clamp(textureSample(aoTexture,materialSampler,uv).r,0.9,1.0);let ambientStrength=getAmbientStrength();let ambientColor=getAmbientColor();let kD=(vec3<f32>(1.0)-F_env)*(1.0-finalMetallic);let diffuseIBL=getDiffuseIndirect(N_geom,in.worldPos);let diffuseAmbient=kD*baseColor*(diffuseIBL*(0.5*iblIntensity)+ambientStrength*0.25+ambientColor*uShadowSettings.ambientIntensity)*ao;let ambient=diffuseAmbient+reflection;let emissiveColor=textureSample(emissiveTexture,materialSampler,uv).rgb*material.params.z;var finalColor=directLighting+ambient+emissiveColor;finalColor=max(finalColor,baseColor*0.045);let sleepingColor=mix(finalColor,finalColor*vec3<f32>(0.7,0.7,0.8),select(0.0,0.3,in.sleepState==1u));let foggedColor=applyFog(sleepingColor,in.worldPos,camera.position);let outColor=vec4<f32>(foggedColor,alpha);$$VELOCITY_CALC$$$$FRAGMENT_RETURN$$}\n".replace(/\$\$LIGHTING_COMMON\$\$/g,o),t),s=a.createShaderModule({code:n});return r.set(l,s),s}static getDebugColliderShader(e){const t=e;let a=this.shaderCache.get(t);a||(a=new Map,this.shaderCache.set(t,a));const r="debug_collider",o=a.get(r);if(o)return o;const l=t.createShaderModule({code:"struct RigidBodyData{position:vec3f,pad0:f32,orientation:vec4f,linearVelocity:vec3f,pad1:f32,angularVelocity:vec3f,pad2:f32,invMass:f32,invInertiaX:f32,invInertiaY:f32,invInertiaZ:f32,friction:f32,rollingResistance:f32,sleepState:u32,sleepTimer:f32,dominance:u32,pad4:u32,pad5:u32,pad6:u32,}struct ColliderData{shapeType:u32,radius:f32,height:f32,pad0:u32,halfExtentsX:f32,halfExtentsY:f32,halfExtentsZ:f32,pad1:u32,}fn quat_to_mat3(q:vec4<f32>)->mat3x3<f32>{let x=q.x;let y=q.y;let z=q.z;let w=q.w;return mat3x3<f32>(vec3<f32>(1.0-2.0*(y*y+z*z),2.0*(x*y+w*z),2.0*(x*z-w*y)),vec3<f32>(2.0*(x*y-w*z),1.0-2.0*(x*x+z*z),2.0*(y*z+w*x)),vec3<f32>(2.0*(x*z+w*y),2.0*(y*z-w*x),1.0-2.0*(x*x+y*y)));}struct Camera{viewProjection:mat4x4<f32>,position:vec3<f32>,padding:f32,};@group(0)@binding(0)var<uniform>camera:Camera;@group(1)@binding(0)var<storage,read>bodies:array<RigidBodyData>;@group(1)@binding(1)var<storage,read>instanceIndices:array<u32>;@group(1)@binding(2)var<storage,read>colliders:array<ColliderData>;struct VertexInput{@location(0)position:vec3<f32>,}struct VertexOutput{@builtin(position)position:vec4<f32>,@location(0)@interpolate(flat)sleepState:u32,}@vertex fn vs_main(input:VertexInput,@builtin(instance_index)instanceIndex:u32)->VertexOutput{var out:VertexOutput;let realIndex=instanceIndices[instanceIndex];let body=bodies[realIndex];let R=quat_to_mat3(body.orientation);var localPos=input.position;if(realIndex<arrayLength(&colliders)){let c=colliders[realIndex];let hx=max(c.halfExtentsX,0.01);let hy=max(c.halfExtentsY,0.01);let hz=max(c.halfExtentsZ,0.01);let r=max(c.radius,0.01);let cylH=max(c.height*0.5-r,0.0);if(c.shapeType==0u){let len=length(input.position);if(len>0.001){localPos=normalize(input.position)*r;}}else if(c.shapeType==1u){localPos=input.position*vec3<f32>(hx,hy,hz);}else if(c.shapeType==2u){localPos=vec3<f32>(input.position.x*r,input.position.y*cylH,input.position.z*r);}else if(c.shapeType==4u){localPos=input.position*vec3<f32>(hx,hy,hz);localPos=localPos+vec3<f32>(0.0,hy*0.5,0.0);}}let modelMatrix=mat4x4<f32>(vec4<f32>(R[0],0.0),vec4<f32>(R[1],0.0),vec4<f32>(R[2],0.0),vec4<f32>(body.position.xyz,1.0));out.position=camera.viewProjection*modelMatrix*vec4<f32>(localPos,1.0);out.sleepState=body.sleepState;return out;}@fragment fn fs_main(input:VertexOutput)->@location(0)vec4<f32>{if(input.sleepState==1u){return vec4<f32>(0.4,0.5,0.7,1.0);}return vec4<f32>(0.0,1.0,0.0,1.0);}\n"});return a.set(r,l),l}static getDebugBodyMarkersShader(e){const t=e;let a=this.shaderCache.get(t);a||(a=new Map,this.shaderCache.set(t,a));const r="debug_body_markers",o=a.get(r);if(o)return o;const l=t.createShaderModule({code:"struct RigidBodyData{position:vec3f,pad0:f32,orientation:vec4f,linearVelocity:vec3f,pad1:f32,angularVelocity:vec3f,pad2:f32,invMass:f32,invInertiaX:f32,invInertiaY:f32,invInertiaZ:f32,friction:f32,rollingResistance:f32,sleepState:u32,sleepTimer:f32,dominance:u32,pad4:u32,pad5:u32,pad6:u32,}struct ColliderData{shapeType:u32,radius:f32,height:f32,pad0:u32,halfExtentsX:f32,halfExtentsY:f32,halfExtentsZ:f32,pad1:u32,}struct Camera{viewProjection:mat4x4<f32>,position:vec3<f32>,padding:f32,};@group(0)@binding(0)var<uniform>camera:Camera;@group(1)@binding(0)var<storage,read>bodies:array<RigidBodyData>;@group(1)@binding(1)var<storage,read>instanceIndices:array<u32>;@group(1)@binding(2)var<storage,read>colliders:array<ColliderData>;struct VertexInput{@location(0)position:vec3<f32>,}struct VertexOutput{@builtin(position)position:vec4<f32>,@location(0)@interpolate(flat)color:vec4<f32>,}@vertex fn vs_main(input:VertexInput,@builtin(vertex_index)vertexIndex:u32,@builtin(instance_index)instanceIndex:u32)->VertexOutput{let _vb_pad=input.position*0.0;var out:VertexOutput;let realIndex=instanceIndices[instanceIndex];let body=bodies[realIndex];let com=body.position.xyz;let L=0.22;let sub=vertexIndex%6u;let line=sub/2u;let end=sub%2u;let center=com;var axis=vec3f(0.0);if(line==0u){axis=vec3f(1.0,0.0,0.0);}else if(line==1u){axis=vec3f(0.0,1.0,0.0);}else{axis=vec3f(0.0,0.0,1.0);}let axisSign=select(-1.0,1.0,end==1u);var worldPos=center+axis*axisSign*L+_vb_pad;worldPos=worldPos+vec3f(f32(colliders[realIndex].shapeType)*0.0);out.position=camera.viewProjection*vec4<f32>(worldPos,1.0);var col=vec4f(1.0,1.0,1.0,1.0);if(line==0u){col=vec4f(1.0,0.25,0.2,1.0);}else if(line==1u){col=vec4f(0.25,0.95,0.3,1.0);}else{col=vec4f(0.25,0.45,1.0,1.0);}out.color=col;return out;}@fragment fn fs_main(input:VertexOutput)->@location(0)vec4<f32>{return input.color;}\n"});return a.set(r,l),l}static getForgePhysicsShadowShaderInstanced(e){const t=e;let a=this.shaderCache.get(t);a||(a=new Map,this.shaderCache.set(t,a));const r="forge_physics_shadow_instanced",o=a.get(r);if(o)return o;const l=t.createShaderModule({code:c});return a.set(r,l),l}static getShadowShaderInstanced(e){const t=e;let a=this.shadowInstancedShaderCache.get(t);a||(a=new Map,this.shadowInstancedShaderCache.set(t,a));const r=`inst_h${C(i)}`,o=a.get(r);if(o)return o;const l=t.createShaderModule({code:i});return a.set(r,l),l}static getShadowShaderInstancedMasked(e){const t=e;let a=this.shadowInstancedShaderCache.get(t);a||(a=new Map,this.shadowInstancedShaderCache.set(t,a));const r=`inst_masked_h${C(n)}`,o=a.get(r);if(o)return o;const l=t.createShaderModule({code:n});return a.set(r,l),l}static getWireframeInstancedShader(e,t){const a=e;let r=this.wireframeShaderCache.get(a);r||(r=new Map,this.wireframeShaderCache.set(a,r));const o=t??{},l="wf_instanced"+(o.hasTangent||o.hasColor?`_${o.hasTangent?"t":""}${o.hasColor?"c":""}`:""),i=r.get(l);if(i)return i;const n=function(e){const t=z(e);return"struct Camera{viewProjection:mat4x4<f32>,position:vec3<f32>,padding:f32,};@group(0)@binding(0)var<uniform>camera:Camera;struct EntityInstance{worldMatrix:mat4x4<f32>,color:vec4<f32>,receiveShadows:f32,alphaCutoff:f32,shadowReceiverBiasMul:f32,meshId:u32,materialId:u32,};@group(2)@binding(0)var<storage,read>entityBuffer:array<EntityInstance>;@group(2)@binding(1)var<storage,read>visibleIndices:array<u32>;struct VertexInput{@location(0)position:vec3<f32>,@location(1)normal:vec3<f32>,@location(2)uv:vec2<f32>$$WIREFRAME_VERTEX_INPUT_EXTRA$$};struct VertexOutput{@builtin(position)position:vec4<f32>,};@vertex fn vs_main(input:VertexInput,@builtin(instance_index)instanceIndex:u32)->VertexOutput{var out:VertexOutput;let realIndex=visibleIndices[instanceIndex];let inst=entityBuffer[realIndex];let worldPos4=inst.worldMatrix*vec4<f32>(input.position,1.0);out.position=camera.viewProjection*worldPos4;return out;}@fragment fn fs_main(_in:VertexOutput)->@location(0)vec4<f32>{return vec4<f32>(0.0,0.0,0.0,1.0);}\n".replace(/\$\$WIREFRAME_VERTEX_INPUT_EXTRA\$\$/g,t)}(o),s=a.createShaderModule({code:n});return r.set(l,s),s}static getWireframeShader(e,t){const a=e;let r=this.wireframeShaderCache.get(a);r||(r=new Map,this.wireframeShaderCache.set(a,r));const o=t??{},l=o.hasTangent||o.hasColor?`wf_${o.hasTangent?"t":""}${o.hasColor?"c":""}`:"wf",i=r.get(l);if(i)return i;const n=o.hasTangent||o.hasColor?function(e){const t=z(e);return E.replace(/\$\$WIREFRAME_VERTEX_INPUT_EXTRA\$\$/g,t)}(o):E.replace(/\$\$WIREFRAME_VERTEX_INPUT_EXTRA\$\$/g,""),s=a.createShaderModule({code:n});return r.set(l,s),s}static clearCache(e){if(e)return this.shaderCache.delete(e),this.shadowShaderCache.delete(e),this.shadowInstancedShaderCache.delete(e),void this.wireframeShaderCache.delete(e);this.shaderCache=new WeakMap,this.shadowShaderCache=new WeakMap,this.shadowInstancedShaderCache=new WeakMap,this.wireframeShaderCache=new WeakMap}}t(D,"shaderCache",new WeakMap),t(D,"shadowShaderCache",new WeakMap),t(D,"shadowInstancedShaderCache",new WeakMap),t(D,"wireframeShaderCache",new WeakMap);const O=async e=>{const t=await fetch(e);if(!t.ok)throw new Error(`Failed to fetch shader: ${e}`);return t.text()};class k{static sourceReaderId(e){let t=this.sourceReaderIds.get(e);return void 0===t&&(t=this.nextSourceReaderId++,this.sourceReaderIds.set(e,t)),t}static deviceCache(e){let t=this.cache.get(e);return t||(t=new Map,this.cache.set(e,t)),t}static registerModule(e,t){this.moduleRegistry.set(e,t)}static async load(e,t,a,r=O){const o=t.startsWith("/")&&a?a(t):t,l=`load:${this.sourceReaderId(r)}:${o}`,i=this.deviceCache(e),n=i.get(l);if(n)return n;const s=await this.loadSource(t,a,r),c=e.createShaderModule({label:t,code:s});return i.set(l,c),c}static async loadSource(e,t=e=>e,a=O){const r=e.startsWith("/")||e.startsWith("http://")||e.startsWith("https://"),o=e.startsWith("/")?t(e):e,l=r?`${this.sourceReaderId(a)}:${o}`:e,i=this.sourceCache.get(l);if(i)return i;let n;if(r)try{n=await a(o)}catch(t){if(t instanceof Error&&t.message.startsWith("Failed to fetch shader:"))throw t;throw Object.assign(new Error(`Failed to fetch shader: ${e}`),{cause:t})}else{const t=this.moduleRegistry.get(e);if(!t)throw new Error(`Shader not found: ${e}. Register package shader paths with ShaderLibrary.registerModule().`);n=t}return this.sourceCache.set(l,n),n}static async loadCombined(e,t,...a){const r=`combined:${t}:${a.join("|")}`,o=this.deviceCache(e),l=o.get(r);if(l)return l;const i=(await Promise.all(a.map(e=>this.loadSource(e)))).join("\n\n"),n=e.createShaderModule({label:t,code:i});return o.set(r,n),n}static loadFromModule(e,t,a="ModuleShader"){const r=`module:${a}`,o=this.deviceCache(e),l=o.get(r);if(l)return l;const i=e.createShaderModule({label:a,code:t});return o.set(r,i),i}static loadCombinedFromModules(e,t,...a){const r=`combined-module:${t}`,o=this.deviceCache(e),l=o.get(r);if(l)return l;const i=a.join("\n\n"),n=e.createShaderModule({label:t,code:i});return o.set(r,n),n}static clearCache(e){e?this.cache.delete(e):(this.cache=new WeakMap,this.sourceCache.clear(),this.sourceReaderIds=new WeakMap,this.nextSourceReaderId=1)}}t(k,"cache",new WeakMap),t(k,"sourceCache",new Map),t(k,"moduleRegistry",new Map),t(k,"sourceReaderIds",new WeakMap),t(k,"nextSourceReaderId",1);const F={};function B(e){const t=e.hasTangent?"t":"",a=e.hasColor?"c":"";return t||a?`std${t}${a}`:"std"}function G(e){let t=32;return e.hasTangent&&(t+=16),e.hasColor&&(t+=16),t}function X(e){const t=G(e),a=[{shaderLocation:0,offset:0,format:"float32x3"},{shaderLocation:1,offset:12,format:"float32x3"},{shaderLocation:2,offset:24,format:"float32x2"}];let r=32;return e.hasTangent&&(a.push({shaderLocation:3,offset:r,format:"float32x4"}),r+=16),e.hasColor&&a.push({shaderLocation:4,offset:r,format:"float32x4"}),{arrayStride:t,attributes:a}}function H(e){return{arrayStride:G(e),attributes:[{shaderLocation:0,offset:0,format:"float32x3"},{shaderLocation:1,offset:12,format:"float32x3"}]}}export{x as STANDARD_INSTANCED_SHADER_VERSION,w as STANDARD_MESHLET_EXPANDED_INSTANCED_SHADER_VERSION,k as ShaderLibrary,h as ShaderType,D as StandardShaderManager,F as VERTEX_LAYOUT_STANDARD,u as applyMainShaderVariantOptions,V as buildBloomExtractInstancedShaderCode,P as buildBloomExtractShaderCode,$ as buildStandardShaderCode,_ as getInstancedShaderCodeHash,L as getMeshletExpandedInstancedShaderCodeHash,H as getShadowVertexBufferLayout,X as getVertexBufferLayout,N as hashStandardInstancedShadowMaskedPassWgsl,T as hashStandardInstancedShadowPassWgsl,y as hashStandardShadowPassWgsl,S as hashWgslShaderSource,B as vertexLayoutToKey};
